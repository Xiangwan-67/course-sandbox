# 功能测试说明书
## 平台绩效方案
1. 说明
平台绩效方案用于定义写手文章原始收益的计算权重（w1~w4）。平台负责人提交方案后，不会立即生效，需管理员审核；审核通过后按“下一轮生效”口径参与轮次结算。
2. 用途：
平台可通过调整点击量、阅读完成量、收藏量、满意度等因子权重，改变写手激励方向；系统据此在轮次结算中计算文章原始收益，并作为后续收益惩罚链路的前置输入。
3. 数据：
3.1 记录方案提交。平台提交后在后台数据库表《平台绩效方案记录》中新增一条记录，字段包含平台、w1~w4、状态（pending）、生效轮次、提交人等。
3.2 记录方案生效。管理员确认后，同表记录状态更新为 active，并写入管理员确认账号与确认时间，供平台页回显“当前生效方案”。
3.3 记录结算结果。轮次结束后，系统在《文章收益结算》表中记录每篇文章使用的 w1~w4、原始收益、惩罚系数与最终收益，文章表《文章》中的报酬字段同步更新。
4. 日志
4.1 方案提交需记录：平台xx提交绩效方案，包含 scheme_id、w1~w4、状态、生效轮次、操作人。日志展示在终端同时存到日志记录中。
4.2 轮次结算需记录：文章收益结算日志包含 article_id、writer、platform、w1~w4、raw、penalty、final 等关键字段，支持对账。

## 撰写测试用例，并进行自动测试
1. 验收标准：
1.1 平台可正常访问绩效方案页面，且可看到“当前生效方案/待审核方案/提交新方案”区域；
1.2 平台提交 w1~w4 后，后端按业务规则写入 pending 记录，生效轮次=当前轮次+1，并写入提交日志；
1.3 管理员确认后，平台页面可回显 active 方案，且 end-round 结算时按该方案权重计算收益并落库；
1.4 全流程数据存储完备、准确，日志记录完备、准确；
2. 测试要求：
2.1 需撰写具体测试用例并说明测试结果；
2.2 对于出错运行情况，记录错误原因和改进方式；
2.3 覆盖常规、特殊、边界、异常场景：包括权重格式非法、权重和为0、非平台角色访问提交、旧接口废弃返回等。
