# 需求说明（当前已实现功能梳理）

> 目标：基于当前代码与已有文档，梳理**写手端**与**用户端**的全部功能、每个功能的操作流程，以及关键数据的**落库点（表/字段）**。  
> 范围：只覆盖当前项目已实现功能（以 `accounts/views.py` + `templates/accounts/*` + `accounts/models.py` 为准）。  
> 备注：本文不描述“平台端/监管端”未来规划；如需可另出文档。

---

## 1. 术语与数据表速查

### 1.1 主要表（`accounts/models.py`）

- **写手**：`写手`（模型 `WriterAccount`）
  - 关键字段：`账号`、`密码`、`所属平台`、`粉丝数`
- **用户**：`用户`（模型 `UserAccount`）
  - 关键字段：`账号`、`密码`、`所属平台`、`关注数`、`禁止登录截止时间`
- **轮次**：`模拟轮次`（模型 `SimulationRound`，单行 `id=1`）
  - 关键字段：`当前轮次`
- **文章**：`文章`（模型 `Article`）
  - 过程字段：`写手账号`、`轮次`、`标题`、`正文`、`标题夸张度_初始值`、`标题夸张度_校准值`、`内容相关度_初始值`、`内容相关度_校准值`
  - 统计字段：`已推送`、`点击量`、`点赞量`、`收藏量`、`吸粉数`、`取关数`、`阅读完成量`、`报酬`
- **推送记录**：`文章推送记录`（模型 `ArticlePush`）
  - 关键字段：`列表类型`（0关注/1发现）、`文章`、`用户`
- **推送明细**：`文章推送明细`（模型 `ArticlePushDetail`）
  - 关键字段：`文章`、`用户`、`是否粉丝`
- **关注关系**：`用户关注写手`（模型 `UserFollowWriter`）
  - 关键字段：`用户`、`写手账号`
- **互动关系表**
  - `用户文章点赞`（`UserArticleLike`）：`用户`、`文章`
  - `用户文章收藏`（`UserArticleCollect`）：`用户`、`文章`
  - `用户文章阅读完成`（`UserArticleReadComplete`）：`用户`、`文章`
- **评论**：`评论`（`Comment`）：`文章`、`内容`、`评论者`、`创建时间`
- **问卷**
  - 切换平台问卷：`切换平台问卷调查`（`PlatformSwitchSurvey`）：`用户编号`、`切换前平台`、`切换后平台`、`轮次`、`切换平台原因`
  - 取关问卷：`取关拉黑账号问卷调查`（`UnfollowSurvey`）：`用户编号`、`写手账号`、`当前轮次`、`取关原因`、`文章编号`

### 1.2 轮次机制（“清空列表但不删库”）

- 当前轮次存于 `模拟轮次(id=1).当前轮次`
- 用户列表页只查询 `文章__轮次 = 当前轮次`
- 推进轮次（进入下一轮）只做 `当前轮次 + 1`，历史数据保留

---

## 2. 写手端功能与流程（Writer）

### 2.1 登录（写手）

- **入口页面**：`templates/accounts/login.html`
- **接口**：`POST /`（`login_view`）
- **流程**：
  1. 输入账号密码提交
  2. 后端优先在写手表中匹配账号
  3. 成功则写入 session 并跳转写手首页
- **数据存储点**：
  - session：`account=<写手账号>`、`role='writer'`
  - DB：无新增/更新

---

### 2.2 写手首页展示（含榜单与入口）

- **页面**：`templates/accounts/writer_home.html`
- **接口**：`GET /writer/`（`writer_home`）
- **展示内容**：
  - “发布文章”按钮（进入发布流程）
  - **文章榜单**：上一轮（n-1）同平台所有文章，按点击量降序（只展示不可点击）
  - **账号榜单**：同平台写手按粉丝数降序
  - “历史文章列表”入口（点击进入 `writer_article_history`）
- **数据读取点**：
  - `模拟轮次.当前轮次`（计算 `prev_round = n-1`）
  - `写手.所属平台`（当前写手平台）
  - `文章`：筛选 `轮次=prev_round` 且 `写手账号 in 同平台写手账号`，按 `-点击量`
  - `写手`：同平台写手按 `-粉丝数`
- **数据存储点**：无（纯读）

---

### 2.3 发布文章（写手发布流程：从点击到发布）

> 发布流程由写手首页的 JS（`fetch`）驱动，多步接口协作完成。

#### 2.3.1 点击“发布文章”

- **接口**：`POST /writer/start-article/`（`writer_start_article`）
- **流程**：
  1. 校验 session `role == 'writer'`
  2. 创建一条新文章记录（仅写入写手账号）
  3. 将 `article_id` 存入 session（当前写手的“正在编辑文章”）
- **数据存储点**：
  - `文章`：新增一行，字段最少 `写手账号`
  - session：`writer_article_id=<文章id>`
  - 日志：记录“写手点击发布文章”

#### 2.3.2 输入主题 + 拖动滑块 + 点击“提交”（生成标题）

- **接口**：`POST /writer/generate-titles/`（`writer_generate_titles`）
- **前端入参**（来自写手页表单）：
  - `topic`（为空则默认“关于喝水与健康的科普”）
  - `title_exaggeration`（标题夸张度滑块值 0-100）
  - `content_relevance`（内容相关度滑块值 0-100）
- **流程**：
  1. 校验写手身份
  2. 读取 topic 和滑块值，做 0-100 截断
  3. **先**将滑块值写入当前文章：`标题夸张度_初始值`、`内容相关度_初始值`
  4. 调用 `sandbox_site/callLLM.py::call_deepseek_api()` 生成标题 JSON
  5. 返回 `{left, center, right}` 三个候选标题给前端展示
- **数据存储点**：
  - `文章.标题夸张度_初始值`、`文章.内容相关度_初始值`（立刻落库）
  - 日志：写手主题与滑块值
  - DB 其他：无

#### 2.3.3 选择标题

- **接口**：`POST /writer/select-title/`（`writer_select_title`）
- **前端入参**：
  - `title_text`（选中的标题文本）
  - `position`（0/1/2 对应 left/center/right）
  - `title_exaggeration_initial`（提交滑块时的值）
- **流程**：
  1. 校验写手身份 + 找到 session 中的当前文章（`writer_article_id`）
  2. 按 position 计算“校准夸张度”：`initial + (position-1)*10`（限制 0-100）
  3. 将标题文本与校准值写入文章
- **数据存储点**：
  - `文章.标题`
  - `文章.标题夸张度_校准值`
  - 日志：写手选择标题（位置 + 标题摘要）

#### 2.3.4 生成正文（根据已选标题 + 内容相关度滑块）

- **接口**：`POST /writer/generate-bodies/`（`writer_generate_bodies`）
- **前端入参**：
  - `article_title`（当前选中的标题）
  - `content_relevance`（内容相关度滑块值 0-100）
- **流程**：
  1. 校验写手身份
  2. 调用 LLM 返回 `{left, center, right}` 三段正文
  3. 返回给前端展示
- **数据存储点**：
  - DB：无（这里只生成候选，不写入文章）
  - 日志：写手请求生成正文（标题摘要 + 滑块值）

#### 2.3.5 选择正文并发布（写入轮次 + 推送）

- **接口**：`POST /writer/select-body/`（`writer_select_body`）
- **前端入参**：
  - `body_text`（选中的正文）
  - `position`（0/1/2）
  - `content_relevance_initial`（滑块值）
- **流程**：
  1. 校验写手身份 + 找到当前文章
  2. 按 position 计算“校准相关度”：`initial + (position-1)*10`（限制 0-100）
  3. 将正文与校准值写入文章，并写入当前轮次 `轮次=_get_current_round()`
  4. 调用 `_do_article_push(article)` 生成推送记录
  5. 返回 `article_id`，前端跳转文章详情（写手视图）
- **数据存储点**：
  - `文章.正文`
  - `文章.内容相关度_校准值`
  - `文章.轮次`
  - 推送表（见下一节）
  - 日志：写手选择正文（位置 + 正文摘要）

---

### 2.4 推送机制（写手发布后，系统将文章推给用户）

- **内部函数**：`_do_article_push(article)`
- **推送规则（当前实现）**：
  - 仅推给与写手同平台的用户（`UserAccount.所属平台 == writer.所属平台`）
  - 粉丝用户：全部进入关注列表（`列表类型=0`）
  - 非粉丝用户：随机抽取 50% 进入发现列表（`列表类型=1`，使用 `SystemRandom().sample`）
- **数据存储点**：
  - `文章推送记录`（`ArticlePush`）：为每个被推送用户写一行（文章+用户唯一）
  - `文章推送明细`（`ArticlePushDetail`）：写入 `是否粉丝`
  - `文章.已推送`：推送记录数统计写回

---

### 2.5 写手文章详情页（写手视图）

- **页面**：`templates/accounts/article_detail.html`（`is_user_view=False`）
- **接口**：`GET /writer/article/<article_id>/`（`article_detail`）
- **展示**：
  - 标题、正文
  - 侧边栏统计：已推送、点击量、点赞量、收藏量、吸粉数、取关数、阅读完成量
  - 评论列表（只读）
- **数据读取点**：
  - `文章`（按 id）
  - `评论`（`article.评论列表`）
- **数据存储点**：无（纯读）

---

### 2.6 写手历史文章列表（不记录日志）

- **页面**：`templates/accounts/writer_article_history.html`
- **接口**：`GET /writer/history/`（`writer_article_history`）
- **展示**：
  - 当前写手的所有文章，按 `轮次 ASC, 创建时间 ASC`
  - 每行：标题、所属轮次、报酬；标题可点击进入写手文章详情
- **数据读取点**：
  - `文章`：`写手账号=当前写手`，按轮次与创建时间排序
- **数据存储点**：无（纯读）

---

## 3. 用户端功能与流程（User）

### 3.1 登录（用户）

- **入口页面**：`templates/accounts/login.html`
- **接口**：`POST /`（`login_view`）
- **流程**：
  1. 输入账号密码提交
  2. 后端匹配到用户账号后，检查 `禁止登录截止时间`（切换平台冷却）
  3. 成功则写入 session 并跳转用户首页
- **数据存储点**：
  - session：`account=<用户账号>`、`role='user'`
  - DB：无新增；仅在切换平台时会更新 `禁止登录截止时间`

---

### 3.2 用户首页：选择平台进入 / 切换平台

- **页面**：`templates/accounts/user_home.html`
- **接口/交互**：
  - `GET /user/platform-check/?target_platform=0|1`（`user_platform_check`）
  - 若需要切换：`POST /user/switch-platform/`（`user_switch_platform`）
- **流程 A：点击的平台 == 当前所属平台**：
  1. 前端请求 `user_platform_check`
  2. 返回 `match=true`
  3. 前端跳转 `GET /user/browse/<platform_id>/`
- **流程 B：点击的平台 != 当前所属平台（切换平台）**：
  1. `user_platform_check` 返回 `match=false`，弹出切换确认
  2. 二次确认后弹出问卷（A/B/C/D 选项，D 需补充文本）
  3. 提交问卷 -> `user_switch_platform`
  4. 后端写问卷、更新用户所属平台与冷却时间、清空 session（强制登出）
  5. 前端跳转回登录页
- **数据存储点**：
  - `切换平台问卷调查`（`PlatformSwitchSurvey`）：新增一行
    - 字段：`用户编号`、`切换前平台`、`切换后平台`、`轮次`、`切换平台原因`
  - `用户`：更新
    - `所属平台 = target`
    - `禁止登录截止时间 = now + 1min`
  - session：`flush()`（登出）
  - 日志：记录用户切换平台原因

---

### 3.3 平台浏览页：关注列表 / 发现列表

- **页面**：`templates/accounts/user_browse.html`
- **接口**：`GET /user/browse/<platform_id>/`（`user_browse`）
- **流程**：
  1. 校验用户身份
  2. 将 `platform_id` 写入 session：`user_browse_platform_id`
  3. 读取当前轮次 `n`
  4. 查询推送记录（只显示当前轮次）：
     - 关注列表：`ArticlePush(用户=当前用户, 列表类型=0, 文章__轮次=n)`
     - 发现列表：`ArticlePush(用户=当前用户, 列表类型=1, 文章__轮次=n)`
  5. 渲染两列文章列表；已访问过的文章在当前会话置灰不可再点
- **数据存储点**：
  - session：`user_browse_platform_id`
  - session：`visited_article_ids`（用户点击进入文章后写入，用于置灰）
  - DB：无新增/更新（纯读）

---

### 3.4 点击进入文章（用户视图）

- **页面**：`templates/accounts/article_detail.html`（`is_user_view=True`）
- **接口**：`GET /user/article/<article_id>/`（`user_article_view`）
- **流程**：
  1. 校验用户身份
  2. 将 `article_id` 记录到 session 的 `visited_article_ids`
  3. `文章.点击量 += 1`
  4. 查询并渲染：
     - 评论列表
     - 当前用户是否已点赞/收藏/阅读完成
     - 当前用户是否关注该写手
- **数据存储点**：
  - `文章.点击量`（更新）
  - session：`visited_article_ids`（更新）
  - 日志：记录“用户点击进入文章”

---

### 3.5 点赞 / 取消点赞

- **接口**：`POST /user/article/<id>/like/`（`user_article_like`）
- **流程**：
  - 若不存在点赞记录：新增点赞记录、`文章.点赞量 += 1`
  - 若已存在：删除点赞记录、`文章.点赞量 -= 1`（下限 0）
- **数据存储点**：
  - `用户文章点赞`（新增或删除）
  - `文章.点赞量`（更新）
  - 日志：点赞/取消点赞

---

### 3.6 收藏 / 取消收藏

- **接口**：`POST /user/article/<id>/collect/`（`user_article_collect`）
- **数据存储点**：
  - `用户文章收藏`（新增或删除）
  - `文章.收藏量`（更新）
  - 日志：收藏/取消收藏

---

### 3.7 阅读完成 / 取消阅读完成

- **接口**：`POST /user/article/<id>/read-complete/`（`user_article_read_complete`）
- **数据存储点**：
  - `用户文章阅读完成`（新增或删除）
  - `文章.阅读完成量`（更新）
  - 日志：阅读完成/取消阅读完成

---

### 3.8 评论

- **接口**：`POST /user/article/<id>/comment/`（`user_article_add_comment`）
- **前端入参**：`content`
- **流程**：
  1. 校验用户身份
  2. 新增评论记录
  3. 返回最新评论数（通过 `article.评论列表.count()`）
- **数据存储点**：
  - `评论`：新增一行（文章、内容、评论者=用户账号）
  - 日志：评论内容（截断摘要）

---

### 3.9 关注写手

- **接口**：`POST /user/article/<id>/follow/`（`user_article_follow`）
- **流程**：
  1. 若已关注则直接返回（不重复写）
  2. 新增关注关系
  3. `文章.吸粉数 += 1`
  4. `写手.粉丝数 += 1`
- **数据存储点**：
  - `用户关注写手`：新增一行（用户、写手账号）
  - `文章.吸粉数`（更新）
  - `写手.粉丝数`（更新）
  - `用户.关注数`：**当前前端关注/取关视图未直接更新**（后台或同步命令可重算；Admin 对关注表变更会同步）
  - 日志：用户关注写手

---

### 3.10 取消关注（两条路径）

#### 3.10.1 手滑快速取消（同页关注又取消）

- **接口**：`POST /user/article/<id>/unfollow/`（`user_article_unfollow`），参数 `quick_unfollow=1`
- **数据存储点**：
  - `用户关注写手`：删除对应关系
  - `文章.吸粉数 -= 1`（下限 0）
  - `写手.粉丝数 -= 1`（下限 0）
  - 日志：快速取消关注

#### 3.10.2 问卷取消关注

- **接口**：同上，但传 `unfollow_reason`
- **数据存储点**：
  - `用户关注写手`：删除对应关系
  - `文章.取关数 += 1`
  - `写手.粉丝数 -= 1`（下限 0）
  - `取关拉黑账号问卷调查`（`UnfollowSurvey`）：新增一行
    - 字段：`用户编号`、`写手账号`、`当前轮次`、`取关原因`、`文章编号`
  - 日志：问卷取消关注（含原因摘要）

---

## 4. 通用功能（写手与用户共用）

### 4.1 退出登录

- **接口**：`GET /logout/`（`logout_view`）
- **数据存储点**：session `flush()`（清空登录态）

---

## 5. 数据重置与账号迁移（辅助流程）

### 5.1 重置动态数据（保留账号）

- **脚本**：`reset_test_data.py`
- **动作**：
  - 对多张表执行 `DELETE FROM ...` 清空数据（文章、推送、互动、评论、问卷、关注关系）
  - `UPDATE 写手 SET 粉丝数=0`
  - `UPDATE 用户 SET 关注数=0`
  - `UPDATE 模拟轮次 SET 当前轮次=1 WHERE id=1`
- **数据存储点**：直接对 `db.sqlite3` 执行真实 DELETE/UPDATE

### 5.2 从 Excel 导入账号

- **命令**：`python sandbox_site/manage.py load_accounts --file 账号管理.xlsx --clear`
- **动作**：
  - 清空写手/用户表（可选 `--clear`）
  - 从 Excel：Sheet1 导写手；Sheet2 导用户（列：账号、密码）

---

## 6. 关键交互注意事项（对后续开发重要）

1. **同一浏览器多标签登录不同角色会互相覆盖 session**：后登录会覆盖 `session['role']`，导致另一个标签页“未登录或非写手/非用户”。并行测试需用不同浏览器/无痕窗口隔离 cookie。
2. **轮次隔离的“清空列表”**：不要删历史数据；用户列表只查当前轮次即可。
3. **并发写（SQLite）**：写手关键写步骤已加锁重试；若扩展更多并发写逻辑需注意锁冲突与幂等性。
4. **行为日志**：关键写手/用户交互已写入 `logs/simulation_actions.log`，便于审计与复盘。

