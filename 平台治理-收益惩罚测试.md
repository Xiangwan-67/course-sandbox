# 功能测试说明书
##收益惩罚
1. 说明
这是平台治理体系中的结算侧惩罚功能。平台发布并生效后，对已被判定为标题党的文章在收益结算阶段施加收益惩罚系数β，用于约束标题党内容的收益激励；该机制与标题党检测、用户举报、绩效规则存在联动关系。
2. 用途：
平台开启此功能后，系统在每轮结束结算时对文章进行原始收益计算，并在满足惩罚条件时按 β 系数扣减收益，验证重点为：配置-审核-发布-生效-轮次结算-结果记录全链路是否正确，且与流量惩罚链路边界清晰（收益惩罚不替代流量惩罚）。
3. 数据：
3.1 记录收益惩罚配置。平台保存参数后，会在后台数据库表《收益惩罚配置》中新增记录（平台、惩罚系数beta、配置状态、提交人与审核信息等）。
3.2 记录功能历史。平台发布/取消收益惩罚，会在后台数据库表《平台治理措施记录》中新增或更新记录（措施类型、生效轮次、取消轮次、状态、配置ID）。
3.3 记录收益结算结果。轮次结算后，会在后台数据库表《文章收益结算》中记录该轮每篇文章结算结果（原始收益、是否应用惩罚、惩罚系数、最终收益、轮次、平台、文章）。
3.4 记录文章收益联动。文章主表《文章》的报酬字段需与《文章收益结算》保持一致，重点校验点击/阅读完成/收藏等基础指标参与计算后，收益惩罚结果是否准确落库。
4. 日志
4.1 结算与惩罚过程需记录：xx平台xx文章在xx轮次结算时，原始收益为xx，是否命中收益惩罚、使用系数β、最终收益为xx；日志展示在终端同时存到日志记录中。
4.2 功能开启/关闭与配置审核需记录：xx平台提交收益惩罚配置、管理员审核通过/驳回、平台发布/取消收益惩罚功能等关键治理操作日志。

##撰写测试用例，并进行自动测试
1. 验收标准：
1.1 单平台下收益惩罚功能可正常配置、审核、发布、取消，且遵循“下一轮生效/下一轮失效”规则；
1.2 收益惩罚功能生效后，对符合条件文章可正确扣减收益；未生效、非标题党或不满足条件文章不误罚；
1.3 并发/批量结算场景下，多篇文章同轮结算时，收益惩罚仍可稳定执行，数据不串写、不丢失；
1.4 全流程数据存储完备、准确，日志记录完备、准确，且可由日志反查数据库记录一致性；
1.5 前端页面状态展示、参数回显、按钮交互与后端实际状态一致（配置状态、发布状态、生效状态、错误提示）。
2. 测试要求：
2.1 根据3中的测试用例，自动测试并说明测试结果；
2.2 对于出错的运行情况，记录错误原因和改进方式；
2.3 覆盖边界情况、异常行为。关注收益惩罚在轮次结算流程中的触发位置与时序边界；
2.4 测试覆盖前端、后端、数据与日志，确保每一类至少包含可回归的自动化用例；
2.5 覆盖收益惩罚与标题党检测、用户举报、绩效规则的组合场景，验证联动结果不冲突。
3. 详细测试用例设计（按场景分类）
3.1 常规情况
3.1.1 前端-配置提交：平台在收益惩罚配置页输入合法beta并提交，页面提示成功并回显当前配置；
3.1.2 后端-审核发布：配置经管理员审核通过后，平台可提交发布，返回成功且记录生效轮次；
3.1.3 结算-命中惩罚：文章被判定为标题党且措施已生效时，结算流程正确应用惩罚系数；
3.1.4 数据-结果落库：生成《文章收益结算》，字段完整且与本轮结算结果一致；
3.1.5 日志-链路留痕：配置、审核、发布、结算、取消各环节均产生日志并可检索。
3.2 特殊情况
3.2.1 组合治理-举报联动：文章由举报流程确认为标题党后，在后续结算中正确触发收益惩罚；
3.2.2 组合治理-绩效联动：在不同绩效权重组合下，收益惩罚仅影响惩罚阶段，不破坏原始收益计算逻辑；
3.2.3 多文章同轮结算：同轮存在标题党与非标题党文章时，分别命中/不命中惩罚，记录独立且结果正确；
3.2.4 多入口操作：治理总览页与收益惩罚配置页切换操作发布/取消，页面状态保持一致。
3.3 边界情况
3.3.1 beta=0：命中惩罚时最终收益归零，结果与预期一致；
3.3.2 beta=1：命中惩罚时不产生额外扣减，结果与未惩罚基线一致；
3.3.3 生效边界：当前轮发布后在当前轮不生效、下一轮生效；取消后当前轮仍按原规则、下一轮失效；
3.3.4 样本边界：点击量/互动量为0或极小值时，结算计算稳定，不出现负数、异常溢出或空记录错写。
3.4 异常情况
3.4.1 重复提交配置：存在待审核/已生效配置时再次提交，接口返回明确错误，前端提示正确；
3.4.2 未审核发布：配置未通过审核时提交发布，接口拒绝并返回可解释错误信息；
3.4.3 重复发布/非法取消：重复发布同措施或对无效状态执行取消时，后端正确拦截；
3.4.4 非法参数与权限异常：beta非法输入、越界输入、无平台权限访问接口时，系统返回合理错误且不污染数据；
3.4.5 日志异常兜底：接口异常或网络失败时，关键失败信息可在日志中定位并支持问题复盘。
