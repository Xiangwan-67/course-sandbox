## 系统介绍及模块划分
本系统是一个用于研究"标题党治理博弈"的沙盘实验平台。实验模拟内容平台生态中各方角色的决策行为，观察不同治理手段组合对标题党内容的抑制效果。

系统包含五类角色：

- **写手**：每轮发布一篇文章，通过选择标题夸张度和内容相关度来制定策略
- **用户**：每轮浏览平台推送的文章，通过点击、评分、举报等行为产生互动数据
- **平台运营方**：调整平台的治理参数（检测力度、惩罚力度等），影响写手的行为成本
- **监管机构**：观察全局生态数据，向平台发出监管要求或处罚
- **实验管理员**：控制实验进程，推进每轮的阶段流转，管理账号，导出数据

| 模块        | 定位                                     |
| --------- | -------------------------------------- |
| **写手模块**  | 内容生产端。写手在此完成文章策略决策和发布，并查看自己的收益与曝光数据    |
| **用户模块**  | 内容消费端。用户在此浏览推送文章，完成点击、阅读、评分、举报、关注等互动行为 |
| **平台模块**  | 治理决策端。平台运营方在此查看生态数据，通过执行六种治理手段（单项或组合执行）治理平台标题党      |
| **监管模块**  | 外部监督端。监管机构在此查看跨平台生态指标，发出监管行动           |
| **管理员模块** | 实验控制端。管理员在此创建实验、推进阶段、管理账号、导出数据         |

## 平台模块角色定位
---
平台模块是整个系统中的**核心治理决策端**。平台角色通过启用或关闭不同治理功能包，并输入对应参数，来影响文章分发、写手收益、账号状态和平台内容生态。

---

## 功能列表

1. 登录
2. 平台首页 / 治理总览（含利润看板）
3. 利润及财务分析
4. 标题党检测功能包
5. 用户举报机制功能包
6. 流量惩罚功能包
7. 收益惩罚功能包
8. 账号管理体系功能包（健康分）
9. 绩效规则功能包
10. 本轮治理结果查看

---

## 功能说明

---

### 1. 登录

平台角色在统一登录页输入账号和密码，登录成功后进入平台首页。

**数据埋点：** 无

---

### 2. 平台首页 / 治理总览

平台角色登录后进入首页。页面分为两个核心区域：

#### 2.1 利润看板区（首页最醒目位置）

展示当前周期的利润汇总情况（详见第3节"利润及财务分析"）。

- 当前周期总利润数值
- 与上一周期的同比增减（绝对值 + 百分比）
- 历史周期及当前周期利润构成趋势图（折线图或柱状图）
- 财务分析摘要入口（点击进入详细分析页）

> **备注：** 利润看板仅在固定周期的最后一轮（如第4轮、第8轮）展示完整周期数据。其余轮次该区域可展示"本周期数据收集中"或展示上一完整周期数据。

#### 2.2 治理状态区

- 当前轮次
- 当前已启用的治理功能包列表
- 各治理功能包当前参数摘要
- 上一轮平台关键结果摘要：
  - 文章总数
  - 被判定为标题党的文章数
  - 用户举报总量
  - 被处罚文章数、被处罚写手数
  - 平台整体点击量、阅读完成量、收藏量
- 进入各治理功能包配置页的入口
- 查看本轮/历史结果的入口

**与其他模块的交互：**
- 首页数据来自写手模块的发文结果、用户模块的行为结果、管理员模块的轮次信息。【预留数据接口】
- 平台启用的策略结果需要同步给用户分发、写手结算、监管查看等流程。【预留数据接口】

**数据埋点：** 无需单独埋点

---

### 3. 利润及财务分析

#### 3.1 功能定位

平台利润看板是平台角色了解自身经营状况的核心工具。利润值由多个计算因子与对应权重系数计算得出，权重系数由管理员在后台针对每个平台单独配置。

平台角色可通过利润看板了解每个周期的盈利状况，并结合财务分析辅助做出治理决策。

#### 3.2 周期定义

- **周期（Cycle）**：由固定数量的轮次构成，默认为4轮/周期，可由管理员配置。
- **利润看板仅在周期最后一轮展示**：例如，4轮为一个周期，则利润看板在第4轮、第8轮、第12轮时展示对应周期的完整利润。非最后一轮时，看板展示"本周期数据收集中"或上一完整周期数据。
- **利润计算范围**：每个周期内所有轮次的累计数据。

#### 3.3 利润计算公式

$$\text{利润} = \sum_{i} (f_i \times w_i)$$

其中，各计算因子及其说明如下：

| 计算因子 | 说明 | 取值方式 |
|:---|:---|:---|
| 文章点击率 | 周期内所有文章点击总量（或点击率均值，具体口径由管理员配置） | 周期内累计 |
| 文章收藏率 | 周期内所有文章收藏总量 | 周期内累计 |
| 文章阅读完成率 | 周期内所有文章阅读完成总量 | 周期内累计 |
| 平台总粉丝数 | 周期结束时所属该平台下的所有用户数量（即所属平台为该平台的用户账户数） | 周期截止时快照 |
| 监管成本 | 本周期内监管机构对平台施加的处罚成本 | 离散分档固定值（见下表） |

**关于文章相关因子：** 若周期内（如4轮内）平台写手共发布10篇文章，则点击率、收藏率、阅读完成率等指标均取这10篇文章对应数据的**加和**。

**关于监管成本（离散分档）：**

| 档次 | 触发条件 | 固定成本值 |
|:---|:---|:---|
| 无 | 本周期内未受任何监管处罚 | 0 |
| 轻度 | 受到警告 | 可配置 |
| 中度 | 受到公开通报/专项整治影响 | 可配置 |
| 重度 | 受到经济罚款 | 可配置 |

> **说明：** 监管成本的具体档次划分和对应固定值，由管理员在后台配置。

#### 3.4 权重系数配置

- 权重系数由管理员在**管理员界面**针对每个平台单独配置
- 不同平台可以拥有不同的权重系数，体现不同平台的经营侧重
- 配置完成后写入数据库，平台页面读取对应平台的权重系数进行计算
- 平台角色本身**不可修改**权重系数，只能查看

**数据库表：平台利润权重配置表（PlatformProfitWeightConfig）**

| 字段 | 说明 |
|:---|:---|
| platform_id | 平台编号 |
| weight_click | 文章点击因子权重 |
| weight_collect | 文章收藏因子权重 |
| weight_finish | 文章阅读完成因子权重 |
| weight_fans | 平台总粉丝数权重 |
| weight_supervision_cost | 监管成本权重（通常为负值） |
| configured_by | 管理员账号 |
| configured_at | 配置时间 |
| effective_round | 生效轮次 |

#### 3.5 利润展示内容

利润看板页面展示以下内容：

- **当前周期总利润**（核心数值，最醒目展示）
- **与上一周期同比变化**（绝对值增减 + 百分比）
- **各因子明细贡献**：每个计算因子的原始值 × 权重系数 = 贡献值，以列表或图表形式展示
- **历史各周期（含本周期）利润趋势图**：折线图或柱状图，展示利润变化趋势
- **财务分析区（预留接口）**：
  - 当前预留展示区域和数据接口
  - 后续由大模型接口实现文字性财务分析内容
  - 分析内容可包括：本周期利润下降原因分析、治理成本与营收的关系解读、下一周期建议等

#### 3.6 后台数据存储

**数据库表：平台周期利润表（PlatformCycleProfitRecord）**

| 字段 | 说明 |
|:---|:---|
| id | 主键 |
| platform_id | 平台编号 |
| cycle_index | 周期编号（第几个周期） |
| cycle_start_round | 周期起始轮次 |
| cycle_end_round | 周期结束轮次 |
| total_click | 周期内文章点击总量 |
| total_collect | 周期内文章收藏总量 |
| total_finish | 周期内文章阅读完成总量 |
| fans_snapshot | 周期截止粉丝数快照 |
| supervision_cost_level | 监管成本档次 |
| supervision_cost_value | 监管成本值 |
| profit_total | 本周期总利润 |
| profit_prev_cycle | 上一周期总利润（用于同比计算） |
| calculated_at | 计算时间 |
| weight_config_snapshot | 本次计算使用的权重系数快照（JSON） |

> **说明：** `weight_config_snapshot`字段以JSON形式存储计算时使用的权重系数，确保历史利润记录可复现，不受后续权重修改影响。

#### 3.7 与其他模块的交互

- 从写手模块文章表中读取文章表现数据（点击量、收藏量、阅读完成量）【预留接口】
- 从用户模块读取平台总粉丝数快照。【预留接口】
- 从监管模块读取本周期监管处罚记录，换算为监管成本。【预留接口】
- 权重系数由管理员模块配置后写入数据库，平台模块直接读取。
- 财务分析内容预留大模型接口。【预留接口】

**数据埋点：**
- 利润计算结果写入`PlatformCycleProfitRecord`表（每周期结束自动触发）
- 平台角色查看利润看板的行为可不单独埋点

---

### 4. 标题党检测功能包

#### 功能定位

用于在文章发布后，平台自动对文章是否属于标题党进行初步判定。该策略是后续流量惩罚、收益惩罚、账号处罚等机制的重要前置条件。

#### 已有实现状态

当前尚未实现，需新建。

#### 操作流程（平台视角）

1. 平台角色进入"标题党检测"配置页
2. 平台角色选择是否启用该功能包
3. 确认启用后，从下一轮开始生效
4. 写手发布文章后，系统自动读取文章的核心参数：
   - 标题夸张度 X
   - 内容相关度 Y
5. 按平台设定规则（管理员后台配置）执行检测，输出结果为bool值：
   - True：判定为标题党
   - False：判定为非标题党
6. 判定结果进入后续处罚 / 分发 / 结算流程

#### API设计（参照已有实现风格）


#### 与其他模块的交互

- 从写手模块读取文章参数 X、Y。【预留接口】
- 将判定结果传递给流量惩罚、收益惩罚、账号管理功能包及监管模块。【预留接口】

#### 数据埋点

主表（PlatformGovernanceMeasure）记录：
- 平台编号（platform_id）
- 策略类型：标题党检测
- config_id：指向 ClickbaitDetectionConfig 表的配置记录ID
- 生效轮次
- 终止轮次（取消时输入）
参数子表（ClickbaitDetectionConfig）记录：
- 判定阈值
- 判定概率值
- 平台编号（platform_id）

**Article 表追加字段**：
is_clickbait（是否被判定为标题党）
clickbait_detected_at（检测轮次）
method_auto_rule（bool值，默认为空）

**标题党检测结果表：**
- 文章ID、所属轮次、所属平台
- 标题夸张度X、内容相关度Y
- 自动检测是否执行
- 自动检测结果（标题党 / 非标题党）

**数据采集时机**：

当创作者发布文章时，若平台已开启标题党检测功能，则对文章进行检测，若文章为标题党，则将检测结果写入 Article 表的 is_clickbait 字段，并将method_auto_rule记录为True。
检测完成后，记录检测时间和检测方式，并将检测结果存储到【标题党检测结果表】
后续流量分配、收益结算、健康分扣减等环节会读取 is_clickbait 字段进行相应处理
---

### 5. 用户举报机制功能包

#### 功能定位

将用户侧的"标题与内容不符"举报纳入平台治理流程。举报累计达到阈值后，触发二次审核，对确认违规的文章和写手追加处罚。

#### 已有实现状态

当前尚未实现，需新建。

#### 操作流程（平台视角）

1. 进入"用户举报机制"配置页，选择是否启用
2. 若启用，配置参数：
   - 举报触发阈值（如达到阅读用户的30%）--平台端仅输入参数，保存至后台数据库表，由管理员在后台进行实际的阈值参数配置。
   - 审核方式（自动高准确率审核 / 管理员人工审核，预留）
   - 举报审核通过后的联动选项：
     - 是否联动触发流量处罚
     - 是否联动触发收益处罚
     - 是否联动触发健康分扣减
3. 保存配置
4. 用户举报累计后，系统判断是否达到阈值
5. 达到阈值则触发审核，审核通过则联动处罚

#### API设计

- 查看举报配置：`GET /platform/governance/report/`
- 保存举报配置：`POST /platform/governance/report/save/`

#### 与其他模块的交互

- 从写手模块接收举报数据、阅读人数数据。【预留接口】
- 将审核结果传递给流量惩罚、收益惩罚、账号管理功能包。【预留接口】
- 若人工审核模式，与管理员模块交互。【预留接口】

#### 数据埋点

主表（PlatformGovernanceMeasure）记录：
- 平台编号（platform_id）
- 策略类型：用户举报机制
- config_id：指向 UserReportConfig 表的配置记录ID
- 生效轮次
- 终止轮次（取消时输入）
参数子表（UserReportConfig）记录：
- 平台编号（platform_id）
- 举报触发阈值（threshold_ratio）
- 审核方式（review_type）：自动或人工

新增 ArticleReport 表：
平台编号（platform_id）
article_id（文章ID）
reporter_user_id（举报人ID）
report_round（举报发生的轮次）
review_status（审核状态：pending/approved/rejected）


Article 表追加字段：

report_count_current_round（本轮被举报次数）
method_user（bool值，默认为空）

数据采集时机：

当用户点击举报按钮时，若平台已开启用户举报功能，则在 ArticleReport 表中记录一条举报记录
每轮结算时，统计每篇文章的举报次数，写入 Article 表的 report_count_current_round 字段
若举报次数 / 阅读次数 ≥ 配置的阈值，触发审核流程，审核结果更新到 ArticleReport 表的 review_status 字段
若审核通过确认为标题党，则更新 Article 表的 is_clickbait = TRUE，并记录 method_user 为True。



---

### 6. 流量惩罚功能包

#### 功能定位

对被判定为标题党的文章施加降权系数α，减少其曝光量。写手不会收到明确通知，只能从文章表现中间接感知。

**学术依据：** 对应"算法降权（Algorithmic Demotion）"概念，采用乘法惩罚模型，文献证明乘法模型优于加法模型（Algorithmic Content Moderation, arXiv 2021）。

#### 已有实现状态

当前尚未实现，需新建。

#### 操作流程（平台视角）

1. 进入"流量惩罚"配置页，选择是否启用
2. 若启用，配置参数：
   - 降权系数 α（取值范围[0,1]）
   - 默认只对自动检测的标题党生效（因为文章推送后无法再撤回流量，所以用户举报的标题党无法进行单篇流量惩罚）
3. 保存配置
4. 文章进入分发流程时，系统判断是否为标题党：
   - 非标题党：降权系数视为1.0，正常分发
   - 标题党：实际曝光量 = 基础曝光量 × α

#### API设计

- 查看流量惩罚配置：`GET /platform/governance/traffic-penalty/`
- 保存流量惩罚配置：`POST /platform/governance/traffic-penalty/save/`

#### 与其他模块的交互

- 从标题党检测功能包读取违规判定结果。【预留接口】
- 将实际曝光权重传递给用户模块的推荐/分发流程。【预留接口】


#### 数据埋点

策略配置数据
主表（PlatformGovernanceMeasure）记录：
平台编号（platform_id）
策略类型：流量惩罚
config_id：指向 TrafficPenaltyConfig 表的配置记录ID
生效轮次
终止轮次（取消时输入）
参数子表（TrafficPenaltyConfig）记录：
平台编号（platform_id）
降权系数 α（alpha）

新增 ArticleTraffic（流量惩罚结果表） 表：
平台编号（platform_id）
article_id（文章ID）
round（轮次）
base_traffic（基础流量）
penalty_applied（是否应用了流量惩罚）
penalty_coefficient（实际惩罚系数，即 1-α）
health_tier_coefficient（健康分档位系数 γ，若启用了健康分体系）
final_traffic（最终流量）

数据采集时机：

当每轮进行流量分配时，若平台已开启流量惩罚功能，则检查文章的 is_clickbait 字段
若 is_clickbait = TRUE ，则应用降权系数 α
计算公式：final_traffic = base_traffic × (1 - α) × （γ）（若启用健康分）
将流量分配结果记录到 ArticleTraffic 表，包括是否应用了惩罚、惩罚系数、最终流量

---

### 7. 收益惩罚功能包

#### 功能定位

对被判定为标题党的文章，在结算时乘以收益惩罚系数β，使写手感知"有阅读量但没有对应收益"。

**学术依据：** 对应"皮古税（Pigouvian Tax）/ Revenue Multiplier"概念，将标题党内容的负外部性内部化（Content Moderation as a Principal-Agent Problem, SSRN 2021）。α（流量惩罚）与β（收益惩罚）功能互补，分别作用于点击前和点击后两个环节（The Economics of Social Media, NBER 2021）。

#### 已有实现状态

当前尚未实现，需新建。

#### 操作流程（平台视角）

1. 进入"收益惩罚"配置页，选择是否启用
2. 若启用，配置参数：
   - 收益惩罚系数 β（取值范围[0,1]，β=0即完全归零）
   - 默认对两种来源的标题党均生效（自动检测or用户举报）
3. 保存配置
4. 文章完成基础收益计算后，系统判断是否为标题党：
   - 非标题党：收益按原收益结算
   - 标题党：最终收益 = 原收益 × β
5. 写手在结算时只能看到最终收益变化，不显示处罚细节

#### API设计

- 查看收益惩罚配置：`GET /platform/governance/revenue-penalty/`
- 保存收益惩罚配置：`POST /platform/governance/revenue-penalty/save/`

#### 与其他模块的交互

- 从检测功能包/举报机制获取标题党判定结果。【预留接口】
- 从绩效规则功能包获取文章原始收益。【预留接口】
- 将最终收益结果传递给写手模块。【预留接口】

#### 数据埋点

策略配置数据
主表（PlatformGovernanceMeasure）记录：
平台编号（platform_id）
策略类型：收益惩罚
config_id：指向 RevenuePenaltyConfig 表的配置记录ID
生效轮次
终止轮次（取消时输入）
参数子表（RevenuePenaltyConfig）记录：
平台编号（platform_id）
惩罚系数 β（beta）

新增 AccountRevenue  表：
平台编号（platform_id）
Account_id （账号ID）
article_id（文章ID）
round（轮次）
base_revenue（基础收益）
penalty_applied（是否应用了收益惩罚）
penalty_coefficient（惩罚系数，即 β）
final_revenue（最终收益）

数据采集时机：

当每轮进行收益结算时，若平台已开启收益惩罚功能，则检查文章的 is_clickbait 字段
若 is_clickbait = TRUE ，则应用惩罚系数 β
计算公式：final_revenue = base_revenue × (1 - β)
将收益结算结果记录到 ArticleRevenue 表，包括是否应用了惩罚、惩罚系数、最终收益




---

### 8. 账号管理体系功能包（健康分）

#### 功能定位

通过"账号健康分"对写手形成长期约束。违规文章触发健康分扣减，影响该写手**后续所有文章**的推流权重，而非仅作用于当前违规文章。

**学术依据：** 对应"声誉机制（Reputation Mechanism）/ 声誉债券（Reputational Bond）"概念（Dynamic Incentives for Content Creators, arXiv 2022）。动态声誉系统优于静态封号机制，可在不同风险偏好的写手之间实现"分离均衡"（Reputation Mechanisms for Content Quality, ACM 2023）。

#### 已有实现状态

**已部分实现。** 当前已完成账号健康分规则的配置发布和取消功能，对应以下API：

- **查看治理措施列表：** `GET /platform/governance/`
  - 展示可选治理措施（当前实现：账号健康分规则）
- **发布治理措施：** `POST /platform/governance/publish/`
  - 入参：`measure_type`
  - 出参：`{ ok, id }`
  - 落库：写入 `PlatformGovernanceMeasure`（平台、发布轮次、**生效轮次=当前轮次+1**、措施类型、发布人账号及措施内容）
  - 日志：写入 `action_log`（平台编号、发布轮次、生效轮次、措施类型）
- **取消治理措施：** `POST /platform/governance/cancel/`
  - 入参：`measure_type`（默认 `account_health_rule`）
  - 出参：`{ ok, id, cancel_round }`
  - 落库：对最新一条规则写入**取消轮次=当前轮次+1**（下一轮起不再执行）
  - 日志：写入 `action_log`（平台编号、取消轮次、措施类型）

#### 待补充/完善内容

当前实现完成了"开关"逻辑，以下参数配置部分需要补充完善：

- 写手初始健康分（当前是否可配置？）
- 每次违规扣分值
- 健康分分档区间定义
- 各档位对应的推流系数γ
- 是否启用信用恢复机制
- 连续N轮无违规后的恢复分值

这些参数需从代码硬编码改为可配置，存入数据库由管理员设置。

#### 操作流程（平台视角）

1. 进入"账号管理体系"配置页，查看当前健康分配置
2. 选择启用或关闭该功能包
3. 若启用，查看管理员已配置的参数：
   - 写手初始健康分
   - 每次违规扣分值
   - 健康分分档区间及各档位推流系数γ
   - 是否启用信用恢复机制
   - 信用恢复轮次N（即文章非标题党N轮后，即可恢复信用分）
   - 每次信用分恢复值P（N轮后，可恢复P分）
4. 点击"发布治理措施"，**生效轮次 = 当前轮次 + 1**
5. 违规文章被确认后，系统执行：
   - 写手账号健康分扣减
   - 根据新健康分更新账号推流系数γ
6. 推流系数γ作用于该写手**后续所有文章**的曝光量：

$$\text{文章最终曝光} = \text{基础曝光} \times \alpha_{\text{文章级}} \times \gamma_{\text{账号级}}$$

7. 若启用恢复机制，写手连续N轮无违规后，健康分自动恢复

#### 注意：文章级处罚与账号级处罚的区分

| 维度 | 流量惩罚(α) / 收益惩罚(β) | 健康分推流系数(γ) |
|:---|:---|:---|
| 作用对象 | **当前违规文章** | **写手后续所有文章** |
| 作用时间 | 当轮结算 | 持续到健康分恢复 |
| 写手感知 | 单篇表现变差 | 所有文章曝光整体下降 |

系统实现和数据记录时必须明确区分这两类处罚，不得合并计算。

#### 与其他模块的交互

- 从自动检测功能包、举报机制获取违规确认结果。【预留接口】
- 将账号当前健康分和推流系数γ传递给用户分发流程、写手首页展示【预留接口】

#### 数据埋点

策略配置数据
主表（PlatformGovernanceMeasure）记录：
平台编号（platform_id）
策略类型：账号健康分体系
config_id：指向 AccountHealthConfig 表的配置记录ID
生效轮次
终止轮次（取消时输入）
参数子表（AccountHealthConfig）记录：
平台编号（platform_id）
初始健康分（initial_score）
每次违规扣减分值（deduction_per_violation）
是否启用恢复机制（recovery_enabled）
恢复所需连续无违规轮次（recovery_rounds_required）
每次恢复的分值（recovery_score_per_cycle）
账号健康分档次（AccountHealthTier）-外键

参数子表（AccountHealthTier）记录：
config_id（关联 AccountHealthConfig 的ID）
平台编号（platform_id）
档位最低分（min_score）
档位最高分（max_score）
推流系数 γ（gamma）
档位标签（label：正常/观察/限流等）
-----------注意，这个表可以直接用现在数据库中已有的【账号健康分档次配置表】

Account 表追加字段：

health_score（当前健康分）
health_tier（当前健康分档位标签）
当前推流系数γ
最近更新轮次


新增 AccountHealthLog 表：（健康分变动记录表）

account_id（账号ID）
round（轮次）
event_type（事件类型：violation 违规扣分 / recovery 恢复加分）
score_change（分数变化，正数为恢复，负数为扣减）
reason（原因：clickbait_detected / consecutive_clean_rounds）
article_id（关联文章ID，若适用）

数据采集时机：

若平台开启账号健康分功能，当文章被判定为标题党（is_clickbait = TRUE）时，在 AccountHealthLog 表记录一条扣分记录，Account 表的 health_score 减去配置的扣分值，并重新计算 health_tier
每轮结算时，检查账号是否连续 N 轮无违规，若满足恢复条件，在 AccountHealthLog 表记录一条恢复记录，Account 表的 health_score 增加配置的恢复值，并重新计算 health_tier
每轮流量分配时，根据账号当前的 health_tier 查询对应的推流系数 γ，应用到该账号所有文章的流量计算中，并记录到 ArticleTraffic 表的 health_tier_coefficient 字段
流量计算公式：final_traffic = base_traffic × (1 - α) × γ（若同时启用了流量惩罚和健康分体系）


---

### 9. 绩效规则功能包

#### 功能定位

定义写手文章报酬的基础计算方式。平台通过调整绩效函数中各因子的权重，引导写手偏向追求点击、阅读完成或收藏，从而改变写手的内容生产策略。

**学术依据：** 绩效函数的权重设计决定了写手的激励结构。纯点击付费模式是平台生态产生标题党的根本原因之一（Clickbait, Engagement, and Platform Responsibility, SSRN）。

#### 已有实现状态

**已部分实现。** 当前实现了3种固定绩效方案的选择与应用，对应以下API：

- **查看绩效方案：** `GET /platform/performance/`
  - 展示可选绩效方案（当前内置3个占位方案），并回显当前生效方案
- **应用绩效方案：** `POST /platform/performance/apply/`
  - 入参：`scheme_code`
  - 出参：`{ ok, id }`
  - 落库：写入 `PlatformPerformanceScheme`（平台、生效轮次=当前轮次、方案编号、发布人账号、方案内容）

#### 待改造内容

**需将当前3种固定方案改为自由参数输入模式**，具体如下：

1. 平台角色在绩效配置页面自由输入各因子权重：
   - w₁：点击量权重
   - w₂：阅读完成量权重
   - w₃：收藏量权重
   - w₄：用户满意度评分权重（预留，初期默认为0）
2. 平台角色提交参数配置
3. **参数不立即生效**，需提交给管理员审核确认后，由管理员在管理员界面完成最终配置生效
4. 管理员确认后，系统将参数写入 `PlatformPerformanceScheme` 并标记为生效状态

> **说明：** 此"提交-管理员确认"机制的意义在于：绩效规则涉及整个实验的收益结算基础，需要管理员（实验设计者）进行最终把关，防止平台角色随意修改导致实验数据失控。

#### 绩效公式

$$\text{文章原始收益} = w_1 \times \text{点击量} + w_2 \times \text{阅读完成量} + w_3 \times \text{收藏量} + w_4 \times \text{满意度均分}$$

$$\text{文章最终收益} = \text{文章原始收益} \times \beta_{\text{收益惩罚系数（如适用）}}$$

#### 三类典型绩效模式（供参考，不再作为固定选项）

| 模式 | w₁ | w₂ | w₃ | 说明 |
|:---|:---|:---|:---|:---|
| 纯点击付费 | 1 | 0 | 0 | 容易激励标题党 |
| 混合付费 | 0.4 | 0.4 | 0.2 | 兼顾点击与质量 |
| 质量导向 | 0.1 | 0.6 | 0.3 | 减少标题党激励 |

> 上述权重仅作为示例参考。实验中实际使用的权重组合由管理员和平台角色协商确定，体现了沙盘中平台角色的治理决策空间。

#### API改造方向

- 查看绩效配置：`GET /platform/performance/`（保留，增加自由参数回显）
- 提交绩效参数（待管理员确认）：`POST /platform/performance/submit/`
  - 入参：`{ w1, w2, w3, w4 }`
  - 落库：写入 `PlatformPerformanceScheme`，状态标记为 `pending`（待管理员确认）
- 管理员确认接口：`POST /admin/performance/confirm/`（属于管理员模块）
  - 将对应记录状态改为 `active`，并记录生效轮次

#### 与其他模块的交互

- 从用户模块读取点击量、阅读完成量、收藏量、满意度评分。【预留接口】
- 将原始收益结果传递给收益惩罚功能包继续处理。【预留接口】
- 将最终结算结果同步到写手模块。【预留接口】
- 权重调整结果需通过管理员模块落地生效。【与管理员模块的接口】

#### 数据埋点

**PlatformPerformanceScheme表改造，追加字段：**
- w₁、w₂、w₃、w₄
- 提交状态（pending / active / cancelled）
- 管理员确认账号
- 管理员确认时间
- 生效轮次

**收益结算表：**
- 文章ID、所属轮次
- 点击量、阅读完成量、收藏量、满意度均分
- 使用的w₁、w₂、w₃、w₄
- 原始收益
- 是否叠加收益惩罚系数β
- 最终收益、结算时间

---

### 10. 本轮治理结果查看

平台角色可在每轮的数据展示阶段查看本轮治理结果。页面展示内容包括：

- 本轮发文总数
- 自动检测命中的标题党文章数
- 用户举报触发审核的文章数
- 最终确认违规的文章数
- 受到流量惩罚的文章数、受到收益惩罚的文章数
- 受到健康分处罚的写手数
- 各功能包参数配置回顾
- 平台整体点击量、阅读完成量、收藏量
- 平台整体写手收益总额
- 用户举报率、切换平台率等生态指标

**与其他模块的交互：**
- 汇总写手模块、用户模块、管理员模块的结果数据。【预留接口】
- 结果数据同步给监管模块查看。【预留接口】

**数据埋点：** 无（纯读取）

---

## 五、全局注意事项

### 1. 功能包必须支持"启用 / 关闭"，且生效时间统一为当前轮次+1

参照已有实现，治理措施的发布和取消均遵循**下一轮生效**的原则，确保当轮行为数据不受中途修改影响，实验数据具有时间一致性。

### 2. 参数必须可配置，不允许写死在代码中

以下参数均应存放在数据库中：
- 举报触发阈值
- 流量降权系数α
- 收益惩罚系数β
- 健康分初始值、扣分值、恢复值、档位与推流系数映射
- 绩效函数权重w₁～w₄
- 利润计算权重系数（各平台独立配置）
- 周期长度（几轮为一个周期）
- 监管成本各档位值

### 3. 处罚来源必须清晰记录

文章被判定为标题党，可能来自：
- 自动检测命中
- 用户举报后审核通过

每次处罚记录必须包含来源字段，避免重复处罚和漏处罚，并支持后续分析哪种治理路径更有效。

### 4. 结算逻辑分层顺序

1. 文章发布
2. 自动检测判定（α文章级降权标记）
3. 分发时应用流量惩罚（α生效）
4. 用户行为产生（点击、完成、收藏、举报）
5. 举报触发二次审核（可能追加标题党标记）
6. 文章原始收益计算（绩效公式）
7. 收益惩罚结算（β生效）
8. 健康分更新（γ调整，影响后续文章）
9. 周期利润计算（周期最后一轮触发）
10. 本轮/本周期结果汇总展示

### 5. 平台利润权重由管理员配置，绩效参数由平台提交管理员确认

两类参数的配置权限需明确区分：
- **利润权重系数**：仅管理员可配置，平台只读
- **绩效函数权重**：平台提交申请，管理员审核确认后生效

好的，以下是补充到平台模块系统需求说明书**注意事项**部分的内容：

---



### 6. 关于各表的设计说明
关于平台编号（platform_id）的重要性
本系统是多平台治理系统，所有策略配置和用户行为数据都必须明确归属于具体平台。因此：
主表 PlatformGovernanceMeasure 必须包含 platform_id 字段，标识该策略配置属于哪个平台
所有参数子表（ClickbaitDetectionConfig、UserReportConfig、TrafficPenaltyConfig、RevenuePenaltyConfig、AccountHealthConfig、AccountHealthTier）都必须包含 platform_id 字段
这样设计的原因：
- 不同平台可以配置不同的策略参数（如平台 A 的降权系数为 0.3，平台 B 的降权系数为 0.5）
- 便于按平台查询和管理策略配置
- 支持跨平台的策略对比分析
