# 功能测试说明书
##流量惩罚
1. 说明
这是平台治理体系中的分发侧惩罚功能。平台发布并生效后，对已被判定为标题党的文章施加流量降权系数，主要作用于发现流量分发环节；该机制与标题党检测、账号健康分（推流系数）存在联动关系。
2. 用途：
平台开启此功能后，写手文章进入推送流程时，系统在文章级别判断是否满足惩罚条件，并按平台配置系数进行降权，验证重点为：配置-审核-发布-生效-推送-结果记录全链路是否正确，且与收益结算链路边界清晰（流量惩罚不替代收益惩罚）。
3. 数据：
3.1 记录流量惩罚配置。平台保存参数后，会在后台数据库表《流量惩罚配置》中新增记录（平台、降权系数alpha、配置状态、提交人与审核信息等）。
3.2 记录功能历史。平台发布/取消流量惩罚，会在后台数据库表《平台治理措施记录》中新增或更新记录（措施类型、生效轮次、取消轮次、状态、配置ID）。
3.3 记录流量处罚结果。文章进入推送流程后，会在后台数据库表《文章流量记录》中记录该轮分发结果（基础流量、是否应用惩罚、惩罚系数、健康分系数、最终流量、轮次、平台、文章）。
3.4 记录文章推送联动。文章推送状态与《文章推送记录/推送明细》需保持一致，重点校验发现流量降权是否准确反映到推送结果，且不影响非目标环节的字段语义。
4. 日志
4.1 推送与惩罚过程需记录：xx平台xx写手xx文章进入推送流程，是否命中流量惩罚、使用系数（alpha/gamma）、最终分发结果为xx；日志展示在终端同时存到日志记录中。
4.2 功能开启/关闭与配置审核需记录：xx平台提交流量惩罚配置、管理员审核通过/驳回、平台发布/取消流量惩罚功能等关键治理操作日志。

##撰写测试用例，并进行自动测试
1. 验收标准：
1.1 单平台下流量惩罚功能可正常配置、审核、发布、取消，且遵循“下一轮生效/下一轮失效”规则；
1.2 流量惩罚功能生效后，对符合条件文章可正确降权；未生效、非标题党或不满足条件文章不误罚；
1.3 并发场景下，多写手同时发文与推送时，流量惩罚仍可稳定执行，数据不串写、不丢失；
1.4 全流程数据存储完备、准确，日志记录完备、准确，且可由日志反查数据库记录一致性；
1.5 前端页面状态展示、参数回显、按钮交互与后端实际状态一致（配置状态、发布状态、生效状态、错误提示）。
2. 测试要求：
2.1 根据3中的测试用例，自动测试并说明测试结果；
2.2 对于出错的运行情况，记录错误原因和改进方式；
2.3 覆盖边界情况、异常行为。关注流量惩罚在文章推送流程中的触发位置与时序边界；
2.4 测试覆盖前端、后端、数据与日志，确保每一类至少包含可回归的自动化用例；
2.5 覆盖流量惩罚与标题党检测、账号健康分的组合场景，验证联动结果不冲突。
3. 详细测试用例设计（按场景分类）
3.1 常规情况
3.1.1 前端-配置提交：平台在流量惩罚配置页输入合法alpha并提交，页面提示成功并回显当前配置；
3.1.2 后端-审核发布：配置经管理员审核通过后，平台可提交发布，返回成功且记录生效轮次；
3.1.3 推送-命中惩罚：文章被判定为标题党且措施已生效时，分发流程正确应用惩罚系数；
3.1.4 数据-结果落库：生成《文章流量记录》，字段完整且与本轮推送结果一致；
3.1.5 日志-链路留痕：配置、审核、发布、推送、取消各环节均产生日志并可检索。
3.2 特殊情况
3.2.1 组合治理-健康分联动：同时启用流量惩罚与账号健康分时，分发结果同时体现两类系数影响；
3.2.2 多角色并发：多个写手同轮发文，分别命中/不命中惩罚，校验各自记录独立、结果正确；
3.2.3 多入口操作：治理总览页与流量惩罚配置页切换操作发布/取消，页面状态保持一致。
3.3 边界情况
3.3.1 alpha=0：命中惩罚时发现流量降到最低边界，结果与预期一致；
3.3.2 alpha=1：命中惩罚时不产生额外降权，结果与未惩罚基线一致；
3.3.3 生效边界：当前轮发布后在当前轮不生效、下一轮生效；取消后当前轮仍按原规则、下一轮失效；
3.3.4 样本边界：非粉丝样本极小或为空时，结果计算稳定，不出现负数、异常溢出或空记录错写。
3.4 异常情况
3.4.1 重复提交配置：存在待审核/已生效配置时再次提交，接口返回明确错误，前端提示正确；
3.4.2 未审核发布：配置未通过审核时提交发布，接口拒绝并返回可解释错误信息；
3.4.3 重复发布/非法取消：重复发布同措施或对无效状态执行取消时，后端正确拦截；
3.4.4 非法参数与权限异常：alpha非法输入、越界输入、无平台权限访问接口时，系统返回合理错误且不污染数据；
3.4.5 日志异常兜底：接口异常或网络失败时，关键失败信息可在日志中定位并支持问题复盘。
