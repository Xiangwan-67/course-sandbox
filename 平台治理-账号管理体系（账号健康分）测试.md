# 功能测试说明书
##账号管理体系（账号健康分）
1. 说明
这是平台治理体系中的账号级长期约束功能。平台发布并生效后，当写手文章被判定为标题党时，系统会按配置扣减写手健康分，并根据健康分档位调整推流系数γ，影响该写手后续文章分发；若开启恢复机制，则在满足条件时自动恢复健康分。
2. 用途：
平台开启该功能后，验证重点为：配置-审核-发布-生效-违规扣分-分档降权-恢复加分-取消失效的全链路是否正确；并验证“默认档位配置（无平台编号）”可作为兜底配置被系统正常读取。
3. 数据：
3.1 记录健康分主配置。平台健康分参数在后台数据库表《账号健康分配置》中记录（初始分、扣分值、恢复开关、恢复轮次、恢复分值、状态、提交人与审核信息）。
3.2 记录健康分档位配置。健康分档位在后台数据库表《账号健康分档位配置》中记录（区间、档位标签、推流系数γ、生效轮次）；当平台专属档位缺失时，应可读取无平台编号的默认档位配置。
3.3 记录功能历史。平台发布/取消账号健康分规则，会在后台数据库表《平台治理措施记录》中新增或更新记录（措施类型、生效轮次、取消轮次、状态、配置ID）。
3.4 记录扣分与恢复审计。违规扣分/恢复加分会在后台数据库表《写手健康分变更审计》中记录（写手、轮次、事件类型、变更值、原因、关联文章）。
3.5 记录账号状态变化。扣分或恢复后，写手主表《写手》的健康分、档位标签、推流系数γ与最近更新轮次应同步更新。
4. 日志
4.1 违规扣分与恢复需记录：xx写手在xx轮次因违规扣减xx分（before/after/tier/gamma），或因连续无违规恢复xx分；日志展示在终端同时存到日志记录中。
4.2 功能开启/关闭与审核流程需记录：平台提交发布账号健康分规则、管理员审核通过/驳回、平台取消规则等关键治理操作日志。

##撰写测试用例，并进行自动测试
1. 验收标准：
1.1 单平台下账号健康分功能可正常发布、取消，且遵循“下一轮生效/下一轮失效”规则；
1.2 功能生效后，标题党文章会正确触发扣分；非标题党文章不误扣；
1.3 健康分档位可正确映射推流系数γ，且支持“无平台编号默认档位表”兜底；
1.4 恢复机制开启后，满足连续无违规条件可正确恢复健康分并记录审计日志；
1.5 全流程数据存储完备、准确，日志记录完备、准确，且可由日志反查数据库记录一致性。
2. 测试要求：
2.1 根据3中的测试用例，自动测试并说明测试结果；
2.2 对于出错的运行情况，记录错误原因和改进方式；
2.3 覆盖边界情况、异常行为。关注健康分扣减下限、发布权限、取消生效边界；
2.4 测试覆盖前端、后端、数据与日志，确保每一类至少包含可回归的自动化用例；
2.5 覆盖账号健康分与标题党检测联动场景，验证触发链路和时序正确。
3. 详细测试用例设计（按场景分类）
3.1 常规情况
3.1.1 前端-治理页展示：平台治理页可展示当前健康分配置与档位列表；
3.1.2 后端-发布生效：发布账号健康分规则后记录待审，管理员通过后按下一轮生效；
3.1.3 违规扣分：规则生效且文章命中标题党时，写手健康分扣减并更新档位/γ；
3.1.4 数据-审计落库：扣分事件写入《写手健康分变更审计》，字段完整；
3.1.5 日志-链路留痕：发布、扣分、恢复、取消等关键环节均有日志。
3.2 特殊情况
3.2.1 默认档位兜底：平台专属档位缺失时，系统可读取无平台编号默认档位；
3.2.2 取消规则生效边界：当轮取消后当轮仍生效，下一轮不再执行扣分；
3.2.3 恢复机制联动：满足连续无违规后自动恢复分值并更新档位γ。
3.3 边界情况
3.3.1 扣分下限：扣分后健康分不得小于0；
3.3.2 档位边界：区间匹配按(下界,上界]规则，边界值落在预期档位；
3.3.3 轮次边界：发布轮次当轮不生效、下一轮生效；取消后下一轮失效。
3.4 异常情况
3.4.1 未提交配置直接发布：返回明确错误且不创建有效规则；
3.4.2 非平台角色发布：返回权限错误且不污染数据；
3.4.3 日志异常兜底：异常路径有可定位失败信息，支持问题复盘。
