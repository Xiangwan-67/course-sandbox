《监管机构-平台巡查功能需求文档》
监管机构可对单个平台发起抽查，抽取平台从xx轮到xx轮之间的n篇文章，检查这些文章中的标题党比例。
# 功能设计
（1）申请提交
监管会在前端的某个界面中点击【平台巡查】功能，但是此次不涉及，先实现后端接口和数据库逻辑。
当选中该功能后，需要跳出一个弹窗：
- 弹窗标题【巡查参数配置】
- 巡查参数框-包括：巡查比例（输入一个0-1范围内的数字，默认值为0.5）、 起始轮次、终止轮次（如起始1，终止3；则巡查范围为1、2、3轮中该平台下的全部文章）
- 最底部两个按钮：[ 取消 ] [ 提交申请] 
点击【提交申请】后，弹窗界面变为：
√申请已提交
等待管理员审核...
备注：由于前端界面是每个平台框中有一个【平台巡查】按钮，因此当监管用户点击该按钮时，自动有一个平台编号/名称字段。
提交后将在后台数据库生成一条申请记录，待管理员审核。
（2）执行巡查并记录结果
当管理员将平台巡查申请批准通过后，进入该平台巡查的执行流程。需查询的字段：
- 用户数（当前轮次下的用户数量）
- 文章数（本次抽查的文章数量：起始轮次到终止轮次之间该平台下写手发布的文章数量总和 * 巡查比例。例如，起始轮次1，终止轮次3，查询数据得到1、2、3轮该平台下共发布100篇文章，巡查比例为0.5，则抽查到的文章数量为50）
- 文章列表（本次抽查的具体文章列表）
- 标题党率（在本次抽查到的文章中，属于【标题党】的文章比例）
注意：对于抽查到的这批文章列表，在判断其是不是标题党文章时，若该文章对应的is_clickbait字段有明确判断值，则直接读取判断（如字段为true，说明是标题党；字段为False，说明是标题党；字段为None等，说明没有明确判断值）；若该文章对应的is_clickbait字段没有明确判断值，则需要启用标题党检测功能（直接复用平台治理-标题党检测）中的检测逻辑即可。
# 业务规则
①同一平台下，如有待审核的平台巡查申请，不允许重复申请；
②终止轮次都必须严格小于当前轮次（即只能查询历史的平台情况）；
③起始轮次必须小于或等于终止轮次，如等于，则相当于只查询一轮。（如起始和终止轮次都为2，则相当于只查询第2轮的平台情况）
# 行为与日志记录
- 当监管用户点击【提交申请】后，在监管机构日志文档中，新增日志记录：监管机构申请平台巡查    申请编号：（申请ID）、 平台编号、平台名称、巡查比例、 起始轮次、终止轮次，同步展示到终端上。
- 《新增管理员专属日志文档，记录管理员操作》当管理员在后台通过或驳回监管用户的巡查申请时，在管理员日志文档中新增日志记录：管理员已批准/驳回监管机构-平台巡查申请  申请编号：ID、平台名称：xx、平台编号：xx
# 数据存储
- 新增《监管机构-平台巡查申请表》，包含字段：申请轮次、 申请ID（唯一主键）、平台名称、平台编号、巡查比例、起始轮次、终止轮次、申请状态（待审核、已驳回、已通过）
- 新增《监管机构-平台巡查表》，包含字段： 平台名称、平台编号、巡查比例、起始轮次、终止轮次、用户数、 抽查文章数、抽查文章列表（存储对应的文章编号列表）、 标题党率、 申请ID（方便追溯申请记录）
- 当监管用户点击【提交申请】后，将对应字段存储到《监管机构-平台巡查申请表》中；
- 当管理员批准通过申请后，将《平台巡查申请表》对应的行数据存储到《平台巡查表》中，并将申请表中申请状态更新为“已通过”；注意，若管理员驳回了申请，则只更新申请状态，《平台巡查表》不添加记录。
- 当巡查执行流程结束后，将巡查得到的结果【用户数】、【抽查文章数】、【文章列表】、【标题党率】存储到《监管机构-平台巡查表》中。