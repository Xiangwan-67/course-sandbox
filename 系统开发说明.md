# 系统开发说明（交接文档）

> 目的：给后续 AI 开发伙伴快速交接当前系统架构、文件职责、功能边界和关键交互流程。  
> 说明：本文件基于当前代码状态整理，不包含未来规划。

## 1. 项目概览

- 技术栈：`Django 6.x` + 模板渲染（HTML + JS `fetch`）+ `SQLite`（`db.sqlite3`）
- 核心业务：写手发布文章 -> 系统推送到用户列表 -> 用户阅读/互动 -> 轮次推进
- 角色：
  - 写手：生成主题、滑块调参、选标题、选正文并发布
  - 用户：浏览关注/发现列表、阅读文章、点赞收藏评论、关注/取关、切换平台
  - 平台负责人：登录平台账号进入平台页（当前为占位页，后续接入利润/治理/绩效）
  - 监管/管理员：通过 Django Admin 查看和维护业务数据（含平台账号维护）

---

## 2. 文件树结构与职责（仅列核心代码文件）

```text
课程沙盘/
├─ sandbox_site/
│  ├─ manage.py                     # Django 命令入口（含路径兼容处理）
│  ├─ settings.py                   # 全局配置：SQLite、模板目录、时区等
│  ├─ urls.py                       # 根路由：/admin/ 与 accounts 子路由
│  ├─ wsgi.py / asgi.py             # 部署入口
│  ├─ callLLM.py                    # 大模型调用封装（写手生成标题/正文）
│  ├─ writer.py                     # 早期领域模型草稿（抽象 Writer），当前主流程未直接调用
│  ├─ user.py                       # 早期领域模型草稿（抽象 User），当前主流程未直接调用
│  └─ article.py                    # 早期领域模型草稿（Article 类），当前主流程未直接调用
│
├─ accounts/
│  ├─ models.py                     # 所有业务数据模型（账号、文章、推送、互动、问卷、轮次）
│  ├─ views.py                      # 核心业务逻辑与接口（写手端、用户端、平台端、轮次、日志触发）
│  ├─ urls.py                       # 业务路由清单
│  ├─ admin.py                      # 后台展示与管理逻辑（含关注记录变更时计数同步、平台账号维护）
│  ├─ action_logger.py              # 动作日志：终端打印 + logs/simulation_actions.log 文件落盘
│  ├─ db_retry.py                   # SQLite 锁冲突重试装饰器（关键写手写操作）
│  ├─ apps.py                       # Django app 配置
│  ├─ tests.py                      # 测试占位（当前无实际用例）
│  ├─ management/commands/
│  │  ├─ load_accounts.py           # 从 账号管理.xlsx 导入写手/用户账号
│  │  ├─ end_round.py               # 轮次 +1（不删库）
│  │  └─ sync_fans_count.py         # 根据关注关系重算写手粉丝数与用户关注数
│  └─ migrations/
│     ├─ 0001 ~ 0013                # 基础模型迭代与扩展
│     ├─ 0014_simulation_round...   # 新增轮次表 + 文章轮次字段
│     ├─ 0015_article_报酬.py        # 文章报酬字段
│     └─ 0016_rename_...关注数.py    # 用户字段「粉丝数」重命名为「关注数」
│
├─ templates/accounts/
│  ├─ login.html                    # 登录页
│  ├─ writer_home.html              # 写手首页（发布流程 + 榜单 + 历史入口）
│  ├─ writer_article_history.html   # 写手历史文章列表
│  ├─ user_home.html                # 用户首页（平台入口 + 切换平台问卷）
│  ├─ user_browse.html              # 用户平台浏览页（关注列表/发现列表）
│  ├─ article_detail.html           # 文章详情（写手视图/用户视图共用）
│  └─ platform_home.html            # 平台首页（占位）
│
├─ reset_test_data.py               # 重置动态数据脚本（保留账号，轮次归 1）
├─ logs/
│  └─ simulation_actions.log        # 操作行为日志文件（运行时追加）
├─ docs/
│  └─ 并发与数据不丢失说明.md        # 并发与重试策略说明
├─ 账号管理.xlsx                     # 账号导入源（Sheet1 写手，Sheet2 用户）
└─ db.sqlite3                       # SQLite 数据库文件
```

---

## 3. 核心数据模型（`accounts/models.py`）

- `WriterAccount`：写手账号、密码、所属平台、粉丝数
- `WriterAccount.健康分`：写手账号健康分（跨轮次累计，默认 100）
- `UserAccount`：用户账号、密码、所属平台、关注数、切换后禁止登录截止时间
- `PlatformAccount`：平台负责人账号、密码、所属平台（用于平台页登录）
- `ProfitWeightConfig`：平台利润计算因子权重配置（管理员维护，全平台一致；监管成本当前默认 0，未来由监管机构注入）
- `PlatformRoundProfit`：平台每轮利润记录（平台+轮次唯一；保存利润、同比增减、因子快照、监管成本字段、财务分析占位）
- `PlatformGovernanceMeasure`：平台治理措施发布记录（平台+轮次；发布后在平台首页通知栏展示）
- `PlatformPerformanceScheme`：平台绩效方案选择记录（平台+生效轮次；当前为占位，后续联动写手报酬函数）
- `AccountHealthLevelConfig`：账号健康分档位配置（区间(下界开,上界闭]，配置发现列表可推流比例；健康档默认建议 0.7 但以后台为准）
- `WriterNoticeRead`：写手平台通知已读记录（用于通知红点）
- `WriterHealthScoreLog`：写手健康分变更审计（标题党命中扣分等）
- `SimulationRound`：当前轮次（单行表，`id=1`）
- `Article`：写手文章主表
  - 过程字段：标题/正文、滑块初始值、校准值、轮次
  - 统计字段：已推送、点击量、点赞量、收藏量、吸粉数、取关数、阅读完成量、报酬
- `ArticlePush`：文章推送记录（关注列表=0 / 发现列表=1）
- `ArticlePushDetail`：推送明细（用户是否粉丝）
- `UserFollowWriter`：用户关注写手关系
- `UserArticleLike / UserArticleCollect / UserArticleReadComplete`：互动关系表
- `Comment`：评论
- `PlatformSwitchSurvey`：切换平台问卷
- `UnfollowSurvey`：取关原因问卷

---

## 4. 当前功能列表

### 4.1 账号与登录

- 登录页按账号先匹配写手，再匹配用户
- 登录页也支持平台账号：匹配 `PlatformAccount` 后进入平台首页 `/platform/`
- 登录后 session 保存 `account`、`role`
- 用户切换平台后 1 分钟内禁止再次登录（到期前会提示）
- 退出登录调用 `session.flush()`

### 4.2 写手端

- 写手首页展示：
  - 发布文章流程入口
  - 平台通知入口（未读时显示红点；进入阅读并标记已读后红点消失）
  - 上一轮同平台文章榜单（按点击量降序）
  - 同平台账号榜单（按粉丝数降序）
  - 历史文章列表入口
- 发布流程：
  1. 点击发布文章 -> 创建 `Article`，记录 `writer_article_id` 到 session
  2. 输入主题 + 选择两个 5 档选项（标题夸张度、内容相关度）并提交（左到右从 1 到 5）
  3. 调用 LLM 生成 3 个标题（left=X-1，center=X，right=X+1；越界则为空字符串），写手选 1 个并写入 `标题夸张度_校准值`
  4. 调用 LLM 生成 3 段正文（left=Y-1，center=Y，right=Y+1；越界则为空字符串），写手选 1 个并写入 `内容相关度_校准值`
  5. 发布后写入当前轮次并推送给同平台用户
    - 若“账号健康分规则”在本轮已生效且未取消：发布时会进行“标题党检测（占位接口）”，命中则健康分扣 10 分，并按扣分后档位配置得到发现列表推流比例
  - LLM 提示词策略：
    - `SYSTEM_CONTENT_TITLES` / `SYSTEM_CONTENT_BODIES`：仅包含固定生成规则、输出 JSON 结构、档次定义；**不包含任何 `{}` 占位符**，不做 `.format()` / `format_map()` 替换
    - `user_content`：仅提供动态数据（例如核心主题、用户指定档次 X/Y 等），避免 JSON 花括号被 Python 格式化误解析导致 500
- 历史文章列表：
  - 仅当前写手可见
  - 按轮次升序
  - 展示标题、所属轮次、报酬，可点击进入文章详情

### 4.3 用户端

- 用户首页：展示平台入口（平台1/平台2）
- 平台匹配检查：
  - 若点击当前所属平台，直接进入浏览页
  - 若切换平台，走“确认弹窗 + 原因问卷 + 提交后强制登出 + 1 分钟冷却”
- 浏览页：
  - 关注列表：推送给自己的关注类文章
  - 发现列表：推送给自己的非关注类文章
  - 仅显示当前轮次推送
  - 已点进过的文章在当前会话置灰不可再点
- 文章详情（用户视图）：
  - 点击进入会增加点击量
  - 支持点赞/取消点赞、收藏/取消收藏、阅读完成/取消、评论
  - 支持关注写手、取消关注（分“手滑快速取消”和“问卷取消”两条路径）

### 4.4 轮次与重置

- `end_round`（HTTP + 管理命令）：
  - 当前轮次 +1
  - 不删除历史数据
  - 用户列表通过 `文章__轮次=当前轮次` 实现“列表清空进入下一轮”
  - 平台利润结算：HTTP 的 `POST /end-round/` 在轮次 +1 前会先结算“本轮”各平台利润，并写入表 `PlatformRoundProfit`（监管成本当前默认 0；保存因子快照与引用权重配置）
- `reset_test_data.py`：
  - 清理动态业务数据（文章、推送、互动、评论、问卷、关注关系）
  - 保留写手/用户账号
  - 将写手粉丝数归 0、用户关注数归 0、轮次重置为 1

### 4.4.1 平台端（平台负责人）

- 平台首页：`GET /platform/`
  - 周期口径：周期长度 P 由 `ProfitWeightConfig.利润展示窗口轮数` 决定（无配置则默认 4）
  - 展示规则：仅展示“完整周期”的利润记录
    - 例：P=4，则第4轮展示1-4，第8轮展示5-8；若当前轮次未到周期末，则展示上一个完整周期
  - 展示值：利润看板最醒目位置展示“本周期总利润”=该周期内每轮 `PlatformRoundProfit.利润` 的求和；并显示较上一周期的增减（上一周期不存在则显示 “—”）
  - 数据来源：`PlatformRoundProfit`（由 `POST /end-round/` 在轮次推进前结算写入）
  - 财务分析：当前为占位展示 `PlatformRoundProfit.财务分析`（JSON），为空则提示“待接入”
  - 通知栏：展示最近发布的治理措施记录（来自 `PlatformGovernanceMeasure`）

- 平台治理：`GET /platform/governance/`
  - 展示可选治理措施列表（当前实现：账号健康分规则）
- 发布治理措施：`POST /platform/governance/publish/`
  - 入参：`measure_type`
  - 出参：`{ ok, id }`
  - 落库：写入 `PlatformGovernanceMeasure`（包含平台、发布轮次、**生效轮次=当前轮次+1**、措施类型、发布人账号与措施内容占位）
  - 日志：写入 `action_log`（平台编号、发布轮次、生效轮次、措施类型）

- 取消治理措施：`POST /platform/governance/cancel/`
  - 入参：`measure_type`（默认 account_health_rule）
  - 出参：`{ ok, id, cancel_round }`
  - 落库：对最新一条规则写入 **取消轮次=当前轮次+1**（下一轮起不再执行）
  - 日志：写入 `action_log`（平台编号、取消轮次、措施类型）

- 平台绩效：`GET /platform/performance/`
  - 展示可选绩效方案（当前内置 3 个占位方案），并回显当前生效方案
- 应用绩效方案：`POST /platform/performance/apply/`
  - 入参：`scheme_code`
  - 出参：`{ ok, id }`
  - 落库：写入 `PlatformPerformanceScheme`（平台、当前轮次作为生效轮次、方案编号、发布人账号、方案内容占位）

### 4.5 运维辅助

- 后台可视化管理：账号、文章、推送、互动、问卷等表均可在 Admin 查看
- 命令：
  - `load_accounts`：Excel 导入账号（写手/用户/平台账号，可重复运行覆盖更新；支持 Sheet「平台」）
  - `sync_fans_count`：按关注关系重算计数
  - `end_round`：推进轮次

### 4.6 日志与审计

- `action_logger.py` 已接入关键动作日志：
  - 写手：发布点击、主题与滑块值、标题选择、正文选择
  - 写手：标题党检测结果、健康分扣分、最终发现列表推流比例、发现列表抽样数量（规则生效时）
  - 用户：进文章、点赞/收藏/阅读完成、评论、关注/取关、平台切换
  - 平台：发布/取消治理措施（账号健康分规则等）
- 日志同时输出到：
  - 终端标准输出
  - `logs/simulation_actions.log`

---

## 5. 交互与通信关系（当前实现）

### 5.1 通信模式

- 前端通过模板渲染 + `fetch` 向后端 HTTP 接口发起请求
- 后端处理后写库并返回 JSON 或页面
- 当前是“请求驱动实时性”（拉取式），不是 WebSocket 推送式

### 5.2 写手发布链路

`writer_home.html`  
-> `POST /writer/start-article/`  
-> `POST /writer/generate-titles/`  
-> `POST /writer/select-title/`  
-> `POST /writer/generate-bodies/`  
-> `POST /writer/select-body/`  
-> 后端 `_do_article_push()` 生成推送  
-> 跳转 `writer/article/<id>/`

补充（账号健康分规则联动）：
- 写手通知：`GET /writer/notices/` 查看平台通知列表；`POST /writer/notices/<id>/read/` 标记已读（红点消失）
- 写手发布文章推送：`writer_select_body` 会根据健康分档位配置得到发现列表推流比例 ratio，并调用 `_do_article_push(article, discover_ratio=ratio)` 对非粉丝随机抽样推送

### 5.3 用户交互链路

`user_home.html`  
-> `GET /user/platform-check/`（必要时）  
-> `POST /user/switch-platform/`（必要时）  
-> `GET /user/browse/<platform_id>/`  
-> `GET /user/article/<id>/`  
-> 互动接口：
- `POST /user/article/<id>/like/`
- `POST /user/article/<id>/collect/`
- `POST /user/article/<id>/read-complete/`
- `POST /user/article/<id>/follow/`
- `POST /user/article/<id>/unfollow/`
- `POST /user/article/<id>/comment/`

---

## 6. 并发与数据一致性（当前策略）

- DB：`SQLite`，`timeout=20`
- 写手关键写操作已加 `@retry_on_db_locked(max_retries=3, delay=0.5)`：
  - `writer_start_article`
  - `writer_generate_titles`
  - `writer_select_title`
  - `writer_select_body`
- 会话隔离：
  - 角色依赖 session 中 `role`
  - 写手当前稿件依赖 `writer_article_id`
- 注意：
  - 同一浏览器登录不同角色会共享同一 session，后登录会覆盖前登录身份
  - 若要同时操作写手和用户，需使用不同浏览器或无痕窗口隔离 cookie

---

## 7. 本地运行与常用命令

```bash
# 1) 激活环境
.\venv\Scripts\Activate.ps1

# 2) 迁移
python sandbox_site/manage.py migrate

# 3) 启动服务
python sandbox_site/manage.py runserver

# 4) 导入账号（覆盖导入）
python sandbox_site/manage.py load_accounts --file 账号管理.xlsx --clear

# 5) 结束本轮
python sandbox_site/manage.py end_round

# 6) 重算写手粉丝数/用户关注数
python sandbox_site/manage.py sync_fans_count

# 7) 重置动态数据（保留账号）
python reset_test_data.py
```

---

## 8. 已知风险与交接提醒

- `sandbox_site/callLLM.py` 当前硬编码了 API Key（安全风险，建议后续改为环境变量）
- `accounts/tests.py` 仍为空，缺少自动化回归测试
- `sandbox_site/writer.py`、`user.py`、`article.py` 为早期抽象模型草稿，当前主业务不依赖它们
- 当前实时性为请求拉取式；如需“页面无刷新实时推送”，需引入 WebSocket/SSE 或轮询

---

## 9. 交接建议（给下一个 AI 伙伴）

1. 先读 `accounts/views.py` + `templates/accounts/*.html` 对齐前后端交互。  
2. 再读 `accounts/models.py` 和最近三条迁移（`0014~0016`）确认数据结构。  
3. 涉及账号导入/重置/轮次推进时优先复用管理命令与脚本，不要直接手改库。  
4. 新功能若涉及并发写，优先沿用 `db_retry.py` 或升级到更强数据库（如 PostgreSQL）。  

