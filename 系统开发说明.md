# 系统开发说明（交接文档）

> 目的：给后续开发者快速接手当前代码状态、模块边界、数据结构和关键结算链路。  
> 范围：截至平台模块任务 P-16 完成后的实际实现，并包含**监管机构发起专项整治行动**（申请—审核—生效）、**监管机构平台巡查**（申请—审核—执行抽查与标题党率统计）、**平台方平台巡查**（平台负责人对本平台发起巡查申请—管理员审核—写入「平台巡查行动表」，与监管侧流程一致、数据表独立）、**专项整治配套自动巡查**（整治结束轮次后的下一轮自动两次巡查，不经管理员审批）、**监管机构经济罚款**（申请—审核—周期利润监管成本）、**平台监测系统**（监管主页各平台巡查快照 +「启动巡查」「罚款」入口；**平台主页**顶部并列展示利润看板与本平台巡查快照 +「启动巡查」，**无罚款**）及**整治进度展示窗口**（主页表格、抽查结果详情页与未读红点）。需求依据见 [监管机构-发起专项整治行动.md](监管机构-发起专项整治行动.md)、[监管-整治进度展示窗口.md](监管-整治进度展示窗口.md)、[监管机构-平台巡查功能.md](监管机构-平台巡查功能.md)、[监管机构-监测系统.md](监管机构-监测系统.md)、[监管机构-专项行动中的自动巡查.md](监管机构-专项行动中的自动巡查.md)、[监管机构-经济罚款功能.md](监管机构-经济罚款功能.md)。

---

## 1. 项目概览

- 技术栈：`Django` + 模板渲染（HTML + JS `fetch`）+ `SQLite`
- 业务主线：写手发文 -> 平台治理生效 -> 推送/互动 -> 轮次结算（收益/健康恢复/周期利润）
- 角色：
  - 写手：选题、选标题、选正文、发布
  - 用户：浏览、互动、举报、关注/取关、切平台
  - 平台负责人：治理功能包配置与发布、利润与治理结果查看
  - 监管机构：登录后进入监管主页查看整治进度；可发起专项整治申请（待管理员审核）
  - 管理员：Django Admin 维护参数与审批（如绩效方案、专项整治申请）

---

## 2. 关键文件职责

- `accounts/models.py`：业务模型（账号、文章、治理措施、各功能包配置、结算记录）
- `accounts/platform_scope.py`：平台目录与监管辖区工具函数（平台名/平台列表/平台 ID 归一化/监管机构负责平台集合）
- `accounts/views.py`：核心业务逻辑（写手/用户/平台端接口 + 结算函数）
- `accounts/round_ops.py`：结束本轮核心逻辑（`perform_end_round`，与 `views.end_round`、管理命令共用）
- `accounts/governance_notices.py`：平台治理措施在**生效轮次**向 `WriterGovernanceNotice` 投递（结束本轮后 + 管理员晚批准补投递）
- `accounts/urls.py`：业务路由
- `accounts/admin.py`：后台管理和动作（含绩效方案确认）
- `accounts/management/commands/end_round.py`：命令行结束本轮（与 HTTP 版本对齐）
- `templates/accounts/*.html`：页面模板（平台治理/绩效/功能包配置/治理结果/监管专项整治/平台监测与巡查等）
- `accounts/action_logger.py`：`action_log`（通用行为审计）、`regulator_action_log`（监管专项）、`platform_patrol_action_log`（平台方发起的平台巡查：申请与执行结果摘要）、`admin_action_log`（管理员操作）、`system_action_log`（系统行为）
- `logs/simulation_actions.log`：通用行为审计日志
- `logs/regulator_actions.log`：监管机构专项整治与**监管机构**平台巡查相关日志
- `logs/platform_actions.log`：平台方平台巡查申请与执行结果（`platform_patrol_action_log`）
- `logs/admin_actions.log`：管理员操作日志（含监管机构/平台方两类平台巡查申请的审批）
- `logs/system_actions.log`：系统行为日志（如专项整治配套自动巡查落库）

---

## 3. 模型清单（P-01 ~ P-16，及监管机构专项整治）

### 3.1 账号与基础业务

- `WriterAccount`：写手账号、平台、粉丝、`健康分`、`health_tier`、`推流系数`、`健康分最近更新轮次`
- `UserAccount`：用户账号、平台、关注数、禁止登录截止时间
- `PlatformAccount`：平台账号（平台编号来自 `settings.SANDBOX_PLATFORMS`，不再固定 0/1）
- `Article`：文章主表（过程字段 + 互动计数字段 + 轮次 + 报酬 + 标题党判定字段）
- `SimulationRound`：当前轮次（单行表）

### 3.2 平台治理与参数子表

- `PlatformGovernanceMeasure`：治理措施发布记录（`config_id` 指向参数子表；管理员审核通过后 `status=active`，写手端展示走收件箱表）
  - `measure_type`：`account_health_rule` / `clickbait_detection` / `user_report` / `traffic_penalty` / `revenue_penalty` / `performance_rule`
- `WriterGovernanceNotice`：写手治理通知**收件箱**（`measure` 外键、`写手账号`、`是否已读`、`read_at`、`投递轮次`）；`写手账号+measure` 唯一。在模拟轮次进入该措施 `生效轮次` 时（`perform_end_round` 内）向该平台全体写手各插入一行；若管理员晚于该轮次才批准，则在批准时补投递。同一条措施不会对同一写手重复投递。
- `WriterNoticeRead`：历史表（旧「措施已读」记录）；当前平台治理通知已读状态以 `WriterGovernanceNotice` 为准。
- `ClickbaitDetectionConfig` / `ClickbaitDetectionResult`
- `UserReportConfig` / `ArticleReport`
- `TrafficPenaltyConfig` / `ArticleTraffic`
- `RevenuePenaltyConfig` / `ArticleRevenueSettlement`（写手单篇结算：`w1`～`w3` 快照、`写作成本系数`（默认 −1）、`写作成本数值`、`原始收益`/`最终收益`、`penalty_*`；因子金额 `因子_点击量` / `因子_阅读完成` / `因子_收藏` / `因子_写作成本`（负值））
- `AccountHealthConfig` / `AccountHealthLevelConfig` / `WriterHealthScoreLog`
- `PlatformPerformanceScheme`（仅 **w1～w3**：点击/阅读完成/收藏权重，**须满足 w1+w2+w3=1**；与写作成本无关；`status`、管理员确认账号/时间）

### 3.3 利润与周期结算

- `ProfitWeightConfig`：按平台独立的利润权重配置（含 `利润展示窗口轮数`）
- `PlatformCycleProfitRecord`：按周期写入利润结果（`platform_id + cycle_index` 唯一）
  - 含周期起止轮次、聚合指标、监管成本快照、利润总额、上一周期利润、权重快照 JSON
  - `监管成本`项与 `监管成本权重` 相乘：`supervision_cost_value` 来自**已生效罚款**（见 `RegulatorFineRecord`），无罚款时为 `0`、级别为「无」

### 3.4 监管机构与专项整治

- `RegulatorAccount`：监管机构登录账号（表「监管机构账号」）
  - `负责平台编号列表`：JSON 列表，如 `[0,1]`，表示该监管机构可对这些平台使用监管功能（辖区互斥由后台保存/导入校验）
- `RegulationActionApplication`：专项整治**申请**（表「监管专项行动申请表」）
  - 一次申请一条；`整治平台编号列表` / `整治平台名称列表` 为 JSON 列表；`申请状态`：`pending` / `approved` / `rejected`
  - `行动编号`：四位递增字符串（如 `0001`），申请与后续正式记录共用同一编号便于追溯
- `RegulationAction`：专项整治**正式记录**（表「监管专项行动表」）
  - **按平台拆行**；`开始轮次` = 申请通过时 `当前轮次 + 1`，`结束轮次` = `开始轮次 + 整治持续轮次 - 1`
  - `状态`：`active`（整治中）/ `finished`（已结束）；查询「整治中」时会将已过结束轮次的 `active` 更新为 `finished`
  - `配套自动巡查已执行`：专项整治结束后，系统在「结束本轮」推进轮次后自动跑两次配套巡查并写入 `PlatformPatrolResult` 后置为 `True`（防重复）
- `AdminBaseConfig`：管理员基础配置（表「管理员基础配置表」，默认 `id=1`）：`自动巡查比例`（配套自动巡查抽样）；`罚款轻微/基础/中等/严格监管成本`（各档次对应数值，审批罚款时快照写入 `RegulatorFineRecord.监管成本数值`，周期结算时参与 `监管成本权重` 加权）；**`写作成本映射`**（JSON，键为内容相关度档位 `"1"`～`"5"`，值为该档位写作成本数值，**全平台共用**；写手通过选题/正文确定相关度档位，结算时 **写作成本项 = 写作成本系数 × 写作成本数值**，默认系数 **−1**，与平台绩效权重无关）
- `RegulatorFineApplication`：罚款**申请**（表「监管机构罚款申请表」）：`申请轮次`、`平台编号`、`平台名称`、`罚款档次`（`light`/`basic`/`medium`/`strict`）、`申请状态`；同一平台仅一条 `pending`
- `RegulatorFineRecord`：罚款**生效记录**（表「监管机构罚款记录表」）：`执行轮次`（审批通过时当前轮次）、`申请ID`、`罚款档次`、**监管成本数值**（审批时从 `AdminBaseConfig` 按档次取数快照）
- `PlatformSpotCheckResult`：平台抽查结果（表「平台抽查结果表」）
  - 与 `RegulationAction` **一对一**（`专项行动` 外键）；`行动编号`、平台编号/名称与专项行动行一致；`是否查看` 布尔
  - 管理员审核通过并生成 `RegulationAction` 时同步创建；历史数据可在监管主页加载「已完成」时惰性补建
- `PlatformPatrolApplication`：监管机构平台巡查**申请**（表「监管机构平台巡查申请表」）
  - `申请轮次`、`平台编号`、`平台名称`、`巡查比例`（0～1）、`起始轮次`、`终止轮次`、`申请状态`（`pending` / `approved` / `rejected`）；主键 `id` 即申请编号
  - 同一平台仅允许存在一条 `pending` 申请（提交接口校验）
- `PlatformPatrolResult`：平台巡查**执行结果**（表「监管机构平台巡查表」）
  - `申请记录`：手动巡查为与申请 **一对一**（`OneToOneField`，可空）；自动巡查无申请，**为空**
  - `巡查类型`：`manual`（手动）/ `auto`（自动）；`专项行动`：自动巡查时指向对应 `RegulationAction`，手动为 `null`
  - 含 `用户数`、`抽查文章数`、`抽查文章列表`（文章 id JSON 列表）、`标题党率`、`执行轮次`（写入结果时的模拟轮次）
  - 手动：管理员在 Admin 审核通过时调用 `views._execute_platform_patrol` → `views._run_platform_patrol_core`
  - 自动：专项整治结束后的下一轮，由 `views._run_regulation_auto_patrol_pair` 连续两次调用 `_run_platform_patrol_core`（抽样与标题党判定逻辑与手动一致）
- `PlatformSelfPatrolApplication`：平台方平台巡查**申请**（表「平台巡查申请表」）
  - 字段语义与 `PlatformPatrolApplication` 对齐；同一平台仅允许一条 `pending`（平台提交接口校验）；与监管机构申请表**互不**占用对方名额
- `PlatformSelfPatrolResult`：平台方平台巡查**执行结果**（表「平台巡查行动表」）
  - `申请记录` 与申请 **一对一**（`OneToOneField`）；含用户数、抽查文章数、抽查文章列表、标题党率、执行轮次等；**不含**监管机构结果表中的「专项行动 / 巡查类型」字段
  - 审批通过：`views._execute_platform_self_patrol`；抽样与标题党判定与监管机构手动巡查共用 `views._compute_platform_patrol_metrics`

---

## 4. 功能与 API（按角色）

### 4.1 写手端

- 发文主链路：
  - `POST /writer/start-article/`
  - `POST /writer/generate-titles/`
  - `POST /writer/select-title/`
  - `POST /writer/generate-bodies/`
  - `POST /writer/select-body/`
- `writer_select_body` 联动：
  - 自动检测写入 `ClickbaitDetectionResult`
  - 若命中且健康分规则生效：按 `AccountHealthConfig.每次违规扣减分值` 扣分
  - 更新写手 `健康分 / health_tier / 推流系数 / 健康分最近更新轮次`
  - **推送发现列表抽样**：`最终比例 = 写手表.推流系数 × α`（α 为流量惩罚系数，未启用或非标题党时为 1）。**基础比例即写手表 `推流系数`**，与后台档位「可推流比例」对齐；未开启账号健康分治理时该字段默认为 1。账号健康分措施在生效轮次进入时，由 `_sync_writer_push_ratios_for_account_health_platform` 按写手当前健康分所在档位批量更新 `推流系数`。
- **平台通知（写手端仅通知「账号健康分」治理措施）**：
  - 触发条件：平台发布 `account_health_rule` 且管理员审核通过（`PlatformGovernanceMeasure.status=active`）后，在到达其 `生效轮次` 时，向该平台下**所有写手**各投递一条通知（收件箱行）。
  - 列表：`GET /writer/notices/`，数据来自 `WriterGovernanceNotice`（`select_related('measure')`），按 `投递轮次` 倒序。该接口仅展示 `measure.措施类型='account_health_rule'` 的通知。
  - 已读：`POST /writer/notices/<收件箱id>/read/`，校验 `写手账号` 后置 `是否已读` 与 `read_at`。
  - 首页「平台通知」红点：存在 `是否已读=False` 的收件箱记录即显示；写手不点击阅读则红点持续存在；点击「我已阅读」后红点消失，通知记录仍保留但样式置灰。
  - 投递时机（非写手访问时懒创建）：
    - 正常：结束本轮推进到新轮次时（`new_round == measure.生效轮次`）投递，见 `accounts/round_ops.py` 调用 `dispatch_governance_notices_for_round(new_round)`。
    - 补投递：管理员晚于生效轮次才批准时，若 `measure.生效轮次 <= 当前轮次`，批准动作会立即补投递（幂等）。
  - 幂等规则：对同一名写手 + 同一条治理措施（同一个 `measure` 记录）只投递一次（`WriterGovernanceNotice.get_or_create(写手账号, measure)`）。若平台取消后重新发布会生成新的 `measure` 记录，因此会投递新的通知。
  - 通知内容展示：通知详情按健康分配置渲染（初始健康分、每次违规扣分、连续合规恢复分、以及健康分档位→推流系数表），数据来自 `AccountHealthConfig` 与 `AccountHealthLevelConfig`。
- **报酬与明细**：
  - 结束本轮时 `_settle_article_revenue`：**绩效收益** = w1×点击量 + w2×阅读完成量 + w3×收藏量（w1+w2+w3=1，来自平台绩效方案）；**写作成本数值**由写手内容相关度档位查 `AdminBaseConfig.写作成本映射`（`accounts/writing_cost.py`）；**收益惩罚前总收益（原始收益）** = 绩效收益 + **写作成本系数**×写作成本数值（系统默认系数 **−1**，即减去映射成本）。若标题党且该轮启用 `revenue_penalty`，`最终收益 = 原始收益 × β`。
  - 个人上轮报酬与因子明细在 **历史文章列表** 中按篇展示；写手首页默认展示 **「我的上一轮表现」** 区块，点击「发布文章」进入发文流程后该块与榜单区一并隐藏，避免干扰发文界面。
  - **历史文章列表**：表格在所属轮次后含 **标题夸张度、内容相关度**（均取校准值），再列互动量与对应收益因子、写作成本、收益扣除、最终收益；无结算轮次对应格为「—」。

### 4.2 用户端

- 浏览与互动：`browse/article/like/collect/read-complete/follow/unfollow/comment`
- 用户举报：`POST /user/article/<id>/report/`
  - 校验平台是否启用举报功能包
  - 同用户同轮同文章防重复
  - 写入 `ArticleReport(pending)`

### 4.3 平台端（配置页）

- 平台首页：`GET /platform/`
  - **顶部左右并列**：左侧为周期利润看板（总利润、同比、因子贡献、历史周期表等）；右侧为**本平台「平台监测系统」**单框（展示逻辑对齐监管主页单平台框：最新一条 `PlatformSelfPatrolResult`、待审核提示、「启动巡查」入口；**无罚款**）
  - 治理总览区（**当前已启用功能包**）：仅展示在当前模拟轮次下 **`_get_effective_governance_measure` 判定为生效** 的措施（`PlatformGovernanceMeasure.status=active`、生效轮次与取消轮次语义与执行侧一致）；参数摘要来自各功能包子表；**不再展示「治理通知栏」**（措施流水以 `GET /platform/governance/` 为准）。
  - 治理结果页入口

- 治理主页面：`GET /platform/governance/`
  - 各措施「已发布」展示条件与详情页一致：最新一条发布申请为 **`active`**、**未安排取消**（`取消轮次` 为空），且 **`measure.config_id` 对应子表仍为 `active`**（绩效规则则要求存在已生效的 `PlatformPerformanceScheme`）；驳回/待审/已取消均显示未发布。
- 发布/取消措施：
  - `POST /platform/governance/publish/`
  - `POST /platform/governance/cancel/`
  - 发布生效轮次 = 当前轮次 + 1
  - 取消轮次 = 当前轮次 + 1

- 功能包配置 API：
  - 标题党检测：`GET/POST /platform/governance/clickbait-detection(/save/)`
  - 用户举报：`GET/POST /platform/governance/report(/save/)`
  - 流量惩罚：`GET/POST /platform/governance/traffic-penalty(/save/)`
  - 收益惩罚：`GET/POST /platform/governance/revenue-penalty(/save/)`
  - 绩效规则：`GET /platform/performance/`、`POST /platform/performance/submit/`；同一平台仅允许存在一条 `status=pending` 的绩效方案，有待审核记录时接口拒绝重复提交，页面隐藏「提交新方案」表单。
  - 旧接口废弃：`POST /platform/performance/apply/`（返回 410）

- 治理结果查看：
  - `GET /platform/round-result/?round=N`
  - 统计本轮发文、检测命中、举报通过、处罚覆盖、互动总量、收益总额（部分指标预留）
- **平台巡查（平台发起）**：
  - `GET /platform/platform-patrol/`：巡查参数配置页；默认 `patrol_ratio=1`，`start_round`/`end_round` 默认为 `max(1, 当前轮次-1)`；当前为第 1 轮时无可巡查「上一轮」，页面提示且不可提交
  - `POST /platform/platform-patrol/submit/`：`Content-Type: application/json`，参数 `platform_id`、`patrol_ratio`、`start_round`、`end_round`；须与登录平台账号的 `所属平台` 一致
  - 业务规则：与 `POST /regulator/platform-patrol/submit/` 相同（`起始轮次 ≤ 终止轮次`、`终止轮次 < 当前模拟轮次`、`0≤比例≤1`、同平台 `pending` 不可重复提交）
  - 写入 `PlatformSelfPatrolApplication(pending)`；写 `platform_patrol_action_log`
  - 管理员审核：Django Admin「平台巡查申请表」或沙盘运营台「监管」标签页下 **「平台巡查（平台发起）」** 待审表
    - 通过：执行 `_execute_platform_self_patrol`，写入 `PlatformSelfPatrolResult`；写 `admin_action_log`（文案区分于监管机构巡查）
    - 驳回：仅更新申请状态；写 `admin_action_log`
  - 执行成功后再次写 `platform_patrol_action_log`（含结果编号、抽查篇数、标题党率、执行轮次）

### 4.4 监管机构端（主页 + 发起专项整治 + 抽查结果详情）

- 登录：`RegulatorAccount` 校验通过后，`session.role = regulator`，跳转**监管主页 `GET /regulator/`**
- **平台监测系统**（`GET /regulator/` 页面上方）：横向排列各平台框；展示**当前模拟轮次**、若有巡查则展示最新一条 `PlatformPatrolResult` 的**用户数**、**文章数（抽查篇数）**、**标题党率**、**期间**（起始–终止轮次）；无记录时上述四项与「期间」均显示「无巡查记录」；**最近更新轮次**为 `当前轮次 − 执行轮次`（0 显示为「本轮」）；**启动巡查**链至 `GET /regulator/platform-patrol/?platform_id=` 填写参数并提交 `POST /regulator/platform-patrol/submit/`；**罚款**按钮（弹窗选档次）提交 `POST /regulator/fine/submit/`（须已有巡查记录；无待审核罚款申请）
- **整治进度（专项整治行动管理）**：`GET /regulator/` 页面下方
  - 摘要：当前轮次、进行中的整治项数（仅统计当前轮次落在 `[开始轮次, 结束轮次]` 且 `状态=active` 的专项行动行）
  - 表格分「进行中 / 已完成」标签；进行中：操作列「等待结束」不可点；已完成：当前轮次 **> 结束轮次** 的行进入「已完成」；进度列「已持续/总持续」按 `min(max(0, 当前轮次-开始轮次), 整治持续轮次)` 展示
  - 已完成行操作「查看抽查结果」：`POST /regulator/platform-spot-check/<id>/open/`（CSRF），仅校验监管登录与抽查记录存在后 `redirect` 至详情页（**不**在此处改已读或写日志）
  - 若 `PlatformSpotCheckResult.是否查看=False`，主页该按钮右上角显示红点（模板变量 `spot_unread`）
  - 抽查结果详情：`GET /regulator/platform-spot-check/<id>/`，纵向展示该专项行动下 `PlatformPatrolResult` 且 `巡查类型=auto` 的记录（按 `id` 升序；通常两条：整治前 / 整治结束当轮）；每条展示抽查轮次（单轮区间，取 `起始轮次`）、巡查比例、执行轮次、用户数、抽查文章数、标题党率（不展示具体文章编号）。**首次 GET 打开**时置 `是否查看=True` 并写 `regulator_action_log`（含当前轮次、行动编号、平台）
- **发起专项整治**：`GET /regulator/special-action/`
  - 展示平台 0/1 可选状态；若某平台当前轮次落在已生效专项整治的 `[开始轮次, 结束轮次]` 内，则该平台不可选（整治中）
  - 整治持续轮次可选：`4` / `8`（默认）/ `12`
  - 整治原因：`定期整治` / `标题党率过高` / `用户投诉激增` / `其他`（选「其他」须填说明）
- 提交申请：`POST /regulator/special-action/submit/`
  - 支持 `Content-Type: application/json`（页面 `fetch`）或表单 POST
  - 写入 `RegulationActionApplication`，状态为 `pending`；**不**直接写入正式表
  - 同一平台在整治中时不允许再次发起
- 管理员审核：Django Admin →「监管专项行动申请表」
  - 动作「审核通过：生成正式专项行动」：申请改为 `approved`，按平台写入 `RegulationAction`，并同步写入 `PlatformSpotCheckResult`
  - 动作「驳回：专项行动申请不通过」：申请改为 `rejected`，不写正式表
- **经济罚款**：管理员批准后在 `RegulatorFineRecord` 落库快照；`views._settle_cycle_profit` 通过 `views._supervision_cost_from_regulatory_fines` 取该平台在周期末轮次前**最近一次**已生效罚款记录（`执行轮次 ≤ end_round`）计入监管成本项
- 管理员审核：Django Admin →「监管机构罚款申请表」通过/驳回；通过写 `RegulatorFineRecord` 并更新申请状态；写 `admin_action_log` 与 `regulator_action_log`
- 说明：专项整治为流程与留痕；配套自动巡查结果在抽查结果页集中展示。
- **平台巡查（后端）**：`POST /regulator/platform-patrol/submit/`
  - `Content-Type: application/json`；参数：`platform_id`、`patrol_ratio`（默认 `0.5`）、`start_round`、`end_round`
  - 业务规则：`起始轮次 <= 终止轮次`；`终止轮次 < 当前模拟轮次`；同一平台有待审核申请时不允许重复提交
  - 写入 `PlatformPatrolApplication(pending)`；写 `regulator_action_log`（申请编号、平台、比例、轮次区间）
- 管理员审核：Django Admin →「监管机构平台巡查申请表」
  - 「审核通过：执行平台巡查并写入结果」：校验通过后执行 `_execute_platform_patrol`，写入 `PlatformPatrolResult`，申请改为 `approved`；写 `admin_action_log`
  - 「驳回：平台巡查申请不通过」：仅更新申请状态，不写结果表；写 `admin_action_log`
- **罚款提交**：`POST /regulator/fine/submit/`（`application/json`：`platform_id`、`fine_tier` 为 `light`/`basic`/`medium`/`strict`）；写 `regulator_action_log`

### 4.5 轮次结算

- HTTP：`POST /end-round/`
- 管理命令：`python sandbox_site/manage.py end_round`
- 两者流程保持一致：
  1. 健康分恢复（若开启）
  2. 本轮文章收益结算
  3. 周期末时执行周期利润结算
  4. 轮次 +1
  5. **写手治理通知投递**：`dispatch_governance_notices_for_round(new_round)` — 对 `status=active` 且 `生效轮次=new_round` 的 `PlatformGovernanceMeasure`，向该平台全体写手写入 `WriterGovernanceNotice`（每名写手每条措施一行，幂等）；若为 **账号健康分** 措施，同轮次对该平台执行 `_sync_writer_push_ratios_for_account_health_platform`（按当前健康分档位更新写手表 `推流系数`）。
  6. **专项整治配套自动巡查**：若本轮刚结算的轮次 `round_to_settle` 等于某条 `RegulationAction.结束轮次`，则当前轮次已为 `结束轮次+1`，对该行动调用两次 `_run_platform_patrol_core`（巡查比例：`AdminBaseConfig.自动巡查比例`；第一次区间 `max(1, 开始轮次-1)` 单轮；第二次区间 `结束轮次` 单轮；`巡查类型=auto`，`申请记录` 为空）；两次均成功则 `配套自动巡查已执行=True`。写 `system_action_log`。

---

## 5. 结算分层顺序（当前实现）

当前代码在“结束本轮”时与发文链路共同形成以下顺序（结合任务约束）：

1. 写手发布正文后触发自动检测（占位逻辑，当前默认 False）
2. 检测结果写 `ClickbaitDetectionResult`，并更新 `Article.is_clickbait`（若执行检测）
3. 推送时：发现列表抽样比例 = 写手表 `推流系数` × 流量惩罚 α（写 `ArticleTraffic`，`health_tier_coefficient` 存推流系数）
4. 用户行为产生（点击、读完、收藏、举报）
5. 举报处理预留流程可在结算阶段确认标题党（来源字段保留）
6. 本轮结束时按绩效权重 w1～w3 算绩效收益，再按写作成本系数与映射值扣写作成本得 **原始收益**（见 §4.1）
7. 若文章标题党且收益惩罚启用，应用 β 得最终收益（写 `ArticleRevenueSettlement`）
8. 违规扣分与轮末恢复更新健康分（写 `WriterHealthScoreLog`）
9. 若命中周期末，按周期聚合并写 `PlatformCycleProfitRecord`
10. 轮次推进到下一轮
11. 专项整治配套自动巡查（若本轮为某专项行动的结束轮次，见 §4.5 第 5 步）

---

## 6. 日志点（`action_log` / `regulator_action_log` / `admin_action_log` / `system_action_log`）

- 通用：`action_log` → `logs/simulation_actions.log`（终端 + 文件，行首时间戳 `[YYYY-MM-DD HH:MM:SS]`）
- 监管专项：`regulator_action_log` → `logs/regulator_actions.log`（同上格式）
  - 监管机构确认发起专项整治、**监管机构申请平台巡查**、管理员审核**专项整治**通过/驳回（既有实现）、**监管机构查看抽查结果**（含当前轮次、行动编号、平台）等
- 平台巡查（平台发起）：`platform_patrol_action_log` → `logs/platform_actions.log`（同上格式）
  - 平台负责人**提交**本平台巡查申请；管理员批准后**执行落库**摘要（申请编号、结果编号、抽查篇数、标题党率、执行轮次等）
- 管理员：`admin_action_log` → `logs/admin_actions.log`（同上格式）
  - **监管机构**平台巡查申请的批准/驳回（申请编号、平台名称、平台编号）
  - **平台发起**的平台巡查申请的批准/驳回（文案含「平台发起」）
  - 罚款申请的批准/驳回（申请编号、平台、档次、执行轮次等）
- 系统：`system_action_log` → `logs/system_actions.log`（同上格式）
  - 专项整治配套自动巡查每次落库成功/失败摘要

当前已覆盖的关键日志包括：

- 写手：
  - 发布流程操作、标题/正文选择
  - 自动检测执行结果
  - 健康分违规扣减明细（before/after/tier/gamma）
- 用户：
  - 阅读互动、关注/取关、切平台、举报提交
- 平台：
  - 各治理功能包参数保存
  - 治理措施发布/取消
  - 绩效方案提交
  - 提交本平台巡查申请（`platform_patrol_action_log`）；审批通过后执行结果再记一条（同上）
- 结算：
  - 单篇收益结算明细
  - 健康分恢复明细
  - 周期利润结算明细（平台、周期、起止轮次、聚合指标、利润）
  - 结束本轮汇总日志
- 监管：
  - 提交专项整治申请（`action_log`）
  - 监管专项关键节点（`regulator_action_log`，见上文）
  - 提交罚款申请、罚款申请通过/驳回（`regulator_action_log`）
- 管理员：
  - 监管机构 / 平台方 两类平台巡查申请审批（`admin_action_log`）
  - 罚款申请审批（`admin_action_log`）
- 系统：
  - 专项整治配套自动巡查（`system_action_log`）

---

## 7. 运维与常用命令

```bash
# 激活环境
.\venv\Scripts\Activate.bat

# 迁移
python sandbox_site/manage.py migrate

# 启动
python sandbox_site/manage.py runserver

# 导入账号（写手/用户/平台/监管机构）
# - 平台账号：Sheet「平台」
# - 监管机构账号：Sheet「监管机构」（列：账号、密码、负责平台；负责平台可填 "[0,1]" 或 "0,1"）
python sandbox_site/manage.py load_accounts --file 账号管理.xlsx --clear --clear-platform --clear-regulator

# 结束本轮
python sandbox_site/manage.py end_round

# 重算粉丝/关注计数
python sandbox_site/manage.py sync_fans_count

# 重置沙盘动态数据（保留写手/用户/平台与监管账号、管理员基础配置、平台利润权重配置；清空文章/结算/治理措施/功能包配置/监管专项与巡查/罚款/周期利润等，并重置模拟轮次与写手健康分）
python reset_test_data.py
```

> 平台目录：由 `sandbox_site/settings.py` 的 `SANDBOX_PLATFORMS` 配置（`(平台编号, 显示名)`），与 `账号管理.xlsx` 中平台编号保持一致。

---

## 8. 已知风险与后续提醒

- `callLLM.py` 仍有密钥硬编码风险，建议改为环境变量
- 自动检测 `is_clickbait` 仍为可插拔占位（默认 False）
- 用户举报处理主流程已预留，若要完全联动需在后续任务中完善调度
- 自动化测试仍不足（`accounts/tests.py` 基本为空）

---

## 9. 交接建议

1. 先读 `accounts/views.py`（结算函数与平台端逻辑集中；平台巡查见 `platform_home` / `_platform_self_monitoring_box` / `platform_self_patrol` / `platform_self_patrol_submit` / `_execute_platform_self_patrol` / `_compute_platform_patrol_metrics`；监管端见 `regulator_home` / `_regulator_monitoring_boxes` / `regulator_platform_patrol` / `regulator_special_action` / `regulator_platform_patrol_submit` / `_execute_platform_patrol` / `platform_spot_check_*`）
2. 再读 `templates/accounts/platform_*.html`（平台端入口和配置页）
3. 然后看 `models.py` 中功能包配置表与结算记录表结构；专项整治见 `RegulatorAccount` / `RegulationActionApplication` / `RegulationAction`；平台巡查见 `PlatformPatrolApplication` / `PlatformPatrolResult`；罚款见 `RegulatorFineApplication` / `RegulatorFineRecord` 与 `AdminBaseConfig` 罚款档位
4. 对轮次与结算改动优先保持 HTTP `end_round` 与管理命令一致
5. 监管账号与辖区可通过 `load_accounts` 从 `账号管理.xlsx` 的 Sheet「监管机构」导入，或在 Admin「监管机构账号」中维护；专项整治审批在「监管专项行动申请表」批量动作中完成；**监管机构**平台巡查审批在「监管机构平台巡查申请表」批量动作中完成；**平台发起**的平台巡查审批在「平台巡查申请表」或沙盘运营台「平台巡查（平台发起）」待审表；罚款审批在「监管机构罚款申请表」；自动巡查比例与各**罚款档次监管成本**在「管理员基础配置」维护（单行）
6. 自动化测试从项目根目录可直接执行 `python -m pytest`（根目录新增 `pytest.ini` 指定 `DJANGO_SETTINGS_MODULE=sandbox_site.settings`）

