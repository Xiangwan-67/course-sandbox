《大型自动化模拟需求文档》

# 需求说明

使用脚本设计46个agent（模拟46个真人）操作系统进行沙盘模拟。

# 账号分配

共20个写手账号、22个用户账号、4个平台账号、2个监管账号。使用单独的Excel文件提供。

# 自动化执行规则（补充）

本节用于把“能跑通”落到可执行、可复盘、可停机的规则上；所有规则以“接口层调用为主（非UI点击为主）”为默认实现路径。

## 轮次推进与轮次屏障（Barrier）

1. 轮次推进采用“管理员在沙盘控制器点击/调用【结束轮次】”作为唯一推进动作（与现有 `/end-round/` 口径一致）。
2. 每一轮推进前，必须满足本轮“角色行为完成条件”（轮次屏障）：
  - 写手agent：本轮要求发布的写手均已完成发布（含必要的标题/内容策略参数）。
  - 用户agent：本轮要求参与的用户均已完成浏览/互动（点赞/收藏/举报/取消关注/阅读完成等按策略执行）。
  - 平台/监管agent：本轮要求触发的巡查/治理申请/审批/专项整治等动作已完成。
3. 若屏障未满足，不允许推进轮次；需在管理员日志中输出“未满足的角色列表/账号列表/缺失动作”，并停止本轮推进。

## 并发读写与一致性底线

1. 允许同一轮内多个agent并发请求（可能命中同一数据表甚至同一行）。
2. 一致性底线：尽可能保证“关键业务事实”不丢失；若出现疑似丢失/覆盖/竞态，必须在日志中以醒目方式记录为 `RACE_CONDITION`（包含轮次、账号、目标对象id、请求动作、期望与实际差异）。
3. 对于必须可对账的关键事件（建议至少覆盖：文章发布、推送命中、用户互动、标题党检测判定、健康分扣/恢复、流量/收益惩罚命中、轮次结算），每个事件应具备可唯一追踪的关键信息字段，便于事后交叉核对。

## 数据库与快照（Snapshot）策略

1. 使用**独立测试库**运行大型自动化（避免污染日常数据）；默认以独立 `db.sqlite3` 文件为测试库。
2. 每轮开始前创建数据库快照：复制测试库文件作为 `round_<N>_pre.sqlite3`（或同等命名规则），以支持失败定位与重放。
3. 所有随机策略（写手标题党概率、用户互动概率、平台选择治理包时机等）必须由固定随机种子驱动：
  - 默认全局seed固定；
  - 允许派生：seed = f(global_seed, round, agent_id) 以确保并发下仍可复现；
  - 日志必须记录每轮使用的seed信息，确保“同一轮可重放”。

## 失败处理与继续策略

1. 任一agent出现以下情况即视为失败并**立即停止**后续轮次：
  - 请求返回非预期（HTTP状态码/业务返回码/关键字段缺失）
  - 轮次屏障无法满足（超时或动作失败）
  - 数据库校验不通过（关键事实对不上）
2. 停止时输出：
  - 失败轮次、失败账号、失败动作、关键对象id
  - 失败前后关键数据快照索引（round_N_pre 快照）
  - 重放建议（是否可从 round_N_pre 重新跑、是否需调整策略/修复代码）
3. 后续是否继续由人工决策（你拍板）后再恢复执行。

## 日志规范（每agent单独日志）

1. 每个agent单独存储行为日志，命名包含：角色、账号、平台编号（如适用）。
2. 每轮开始必须打印醒目轮次头，例如：`------------------- 当前为第N轮 (seed=...) -------------------`
3. 每条行为日志至少包含：轮次、账号/角色、动作名、关键对象（文章id/写手id/用户id/平台id等）、动作结果（成功/失败/返回码）。

## 验收口径（跑通定义）

1. 连续跑满100轮（或指定轮数）且无失败停机。
2. 每轮关键事件可对账：日志与数据库事实一致（至少覆盖本节“一致性底线”的关键事件）。
3. 各模块功能点按本文“模拟目标”章节口径均在长跑过程中被触发并通过校验。

# 模拟目标

## 写手角色

1. 主页各页面正常展示、数据展示无误；
2. 发布文章功能可正常使用，模拟测试中可简化，跳过调大模型API的过程；
3. 写手文章收益可以正常计算；
4. 平台发布的治理通知，写手可以正常收到并正常读，已读标志会正常更新。

## 用户角色

1. 文章推送给用户时比例正常，能够正常推送；
2. 用户主页的发现列表和关注列表可以正常显示文章；
3. 用户点击文章后，各功能（取消关注、点赞、举报、阅读完成、收藏）可正常使用，且点赞、阅读完成、收藏点击后可取消；对应的文章表现数据可正常增减）
4. 用户在登录后可切换平台，切换时有时间限制；
5. 用户取消关注、切换平台时能正常跳出询问问卷；

## 平台角色

1. 平台巡查功能可正常使用，内部巡查；
2. 平台利润能够正常计算，并按周期显示在平台首页最顶端，且一些数据分析(如利润同比、明细)等能正常显示；
3. 平台绩效方案能够正常申请、生效，且能正常影响写手的文章收益计算公式；不同平台间申请不同类型的绩效方案（分别将不同的因子取最高权重0.8）；
4. 平台各治理功能（标题党检测、用户举报机制、账号健康分、流量惩罚、收益惩罚）均能够正常申请、生效，并在系统流程中正常发挥作用，各功能的正常发挥作用标准如下：

①标题党检测：（平台开启此功能后）该平台下的写手发布文章后，触发此流程。根据标题党检测配置规则，判断该文章是否为标题党，并更新文章表字段，新增标题党检测记录等；如平台未开启此功能，不触发。如平台开启此功能但管理员后台无配置，需提示【当前无配置！】。
②用户举报：平台开启此功能后，当平台下某一文章的用户举报比率达到阈值，则启动标题党检测流程，若此时文章已被记录为标题党，则只更新文章表的methor_user字段；若此时文章未被记录为标题党，则该机制自动调用标题党检测函数进行判定，并更新相应文章字段，记录检测结果。>> 此流程因在轮次结束时，文章收益结算前进行。
③账号健康分体系：平台开启此功能后，当平台下某一文章被检测为标题党（无论是来自自动标题党检测功能，还是来自用户举报功能），触发扣分机制；如账号行为连续合规，触发恢复机制。扣分和恢复健康分，都需要更新账号的健康分值以及对应的推流系数。>> 若平台不开启此功能，账号的健康分字段始终为100，不发生扣罚也不发生恢复。一些特殊情况：该功能严格依赖[标题党检测]，因此若平台未开启标题党检测或用户举报功能，则账号健康分功能即使开启，应该也发挥不了作用。（因为没有文章的is_clickbait为true）
④流量惩罚：平台开启此功能后，当平台下的写手发布文章后、推送给用户前，会先触发此功能。对标题党文章施加流量降权（非标题党文章流量不受影响），影响文章推送比例。>> 若平台不开启此功能，则文章之间推送比例的差异只会来自健康分功能。一些特殊情况：该功能严格依赖[标题党检测]，因此若平台未开启标题党检测功能（不含用户举报功能，因为用户举报标记标题党在轮次结束时，而流量惩罚在文章推送前，在轮次初），则流量惩罚功能即使开启，应该也发挥不了作用。（因为没有文章的is_clickbait为true）
⑤收益惩罚：平台开启此功能后，当轮次结束进入文章结算流程时，写手文章先按平台当前生效的绩效方案结算收益，计算出基础收益后，触发此功能，按照配置的惩罚系数，对标题党文章收益进行扣减。 >> 若平台不开启此功能或文章不是标题党，则文章不会有收益惩罚。注意：该功能严格依赖[标题党检测]，因此若平台未开启标题党检测或用户举报功能，则收益惩罚功能即使开启，应该也发挥不了作用。（因为没有文章的is_clickbait为true）
5. 按模拟轮次推进顺序，平台治理的功能应该按照如下顺序：先标题党检测类（标题党检测或用户举报）、再流量惩罚类（健康分、流量惩罚）、再收益类（收益惩罚）>> 此为可正常生效的测试类型，其中某平台按此过程正常进行。但是也需要选择一个平台agent进行异常测试（在不开启前置功能[标题党检测]或[用户举报]前，直接开启健康分、流量惩罚、收益惩罚等治理功能）

## 监管机构角色

1. 监测系统显示的平台必须为自己辖区内的全部平台；
2. 监测系统中的平台巡查功能能够正常使用，且巡查结果准确；
3. 监测系统中的罚款功能能够正常使用，且能够体现到平台利润的[监管成本]部分；
4. 发起专项整治功能能够正常使用，且被整治的平台会收到整治通知；
5. 发起专项整治功能中的两次配套自动巡查工作能够正常使用，且巡查结果准确，且巡查结果记录与监管用户手动执行的平台巡查可区分；
6. 专项整治进度窗口显示准确、无遗漏，操作字段准确，[已完成]的整治，[查看抽查结果]页面能够正常显示，且抽查结果准确。

## 管理员角色

1. 点击[结束轮次]（推进到下一轮次）后，能够正常执行用户举报>>收益惩罚>>收益结算等轮次末需执行的流程；
2. 各申请审批功能可正常使用，通过的功能可生效且正常运行，驳回的功能不生效。

## 目标

1. 每个agent的日志中需以醒目方式体现轮次变化。如“-------------------当前为第1轮-------------------”；
2. 每个agent的具体行为需记录在日志中，且包含行为对应的关键对象（如用户浏览文章，需记录文章id、写手id、轮次）；
3. 全流程数据埋点准确，存储字段无遗漏、准确；

## 模拟进度

平台1：写手一开始就使用标题党，平台按正常功能治理流程选择治理包，配置绩效方案；
平台2：写手一开始正常，后来（4轮后）某一个写手开始使用标题党策略，其余写手2-3轮后开始模仿，（写手们都开始用标题党策略后）平台先按非正常流程选择治理包，4-8轮后改用正常流程治理；
平台3：写手一直不使用标题党平台按正常功能治理流程选择治理包，配置绩效方案；
平台4：写手一开始就使用标题党，平台先按非正常流程选择治理包，4-8轮后改用正常流程治理。

# 逐轮策略设计（待你审阅）

说明：下述“策略”用于驱动46个agent在每轮要做什么。默认以接口层调用实现，因此策略描述聚焦“业务动作与参数”，而不是页面点击细节。

## 全局策略参数（建议默认值，可调整）

1. 写手每轮发布：默认每写手每轮发布 1 篇。
2. 写手标题/正文策略参数（用于触发标题党检测）：
  - 系统标题党判定口径（实现侧）：当“标题夸张度_校准值 >= 阈值X 且 内容相关度_校准值 < 阈值Y”判定为标题党（阈值默认 X=4, Y=3；数值通常为 1～5 档）。
  - 自动化中以 1～5 档生成参数：
    - 标题党档：标题夸张度=5、内容相关度=1（确保命中）
    - 正常档：标题夸张度=2、内容相关度=4（确保不命中）
3. 用户每轮阅读：默认每用户每轮阅读 K 篇（建议 K=5；若性能允许可提高），并按概率触发互动（点赞/收藏/举报/取消关注/阅读完成）。
4. 用户互动默认概率（可按平台/轮次覆盖）：
  - 点赞 like_p=0.20
  - 收藏 collect_p=0.10
  - 阅读完成 read_complete_p=0.35
  - 取消关注 unfollow_p=0.02（触发问卷）
  - 切换平台 switch_platform_p=0.02（触发问卷；需满足系统时间限制）
  - 举报 report_p=0.02（仅在启用举报功能包的平台有效；同用户同轮同文章不可重复）
5. 举报达阈值策略（用于覆盖“用户举报机制”）：
  - 实现侧口径：举报达阈值以 `举报数 / 阅读次数(点击量)` 与 `UserReportConfig.举报触发阈值` 比较（默认 0.30），达阈值后按 `审核方式` 自动/人工确认。
  - 自动化在“触发轮次”对少数目标文章做“集中举报”，将 `ratio` 推到 ≥0.35（留余量），其余轮次维持低水平以测试非触发路径。
6. 随机与可复现：所有概率与抽样均由固定seed驱动（每轮/每agent派生），并在日志记录每轮seed与触发统计。

## 覆盖矩阵（确保每个功能都被测到）

- 标题党检测：命中/不命中、未启用时不触发、启用但无有效配置时提示/返回错误口径。
- 流量惩罚：启用后标题党降权 vs 非标题党不降权；未启用不影响；前置缺失（未启用检测）时不应误生效。
- 健康分：标题党触发扣分、连续合规触发恢复；未启用时写手健康分保持 100 且推流系数不应被改写。
- 收益惩罚：标题党文章结算应用 β；非标题党不应用；未启用不应用；前置缺失时不应误生效。
- 用户举报：低概率不达阈值路径；集中举报达阈值路径（auto/manual 两种审核方式均覆盖）；并覆盖“举报记录防重复”。
- 绩效方案：每平台不同权重（某因子权重取高），提交/待审/生效、重复提交被拒、权重和不为1被拒。
- 平台巡查/罚款/专项整治：监管端申请与管理员审批链路、专项整治结束后下一轮自动两次巡查写入、抽查结果页已读红点。
- 管理员：审批通过/驳回两种结果、结束轮次触发轮末结算链路与后置任务。

## 写手agent逐轮策略（框架）

- 基础动作：登录 -> 发布文章（标题夸张度/内容相关度按策略设定）-> 查收益/通知（如策略需要）-> 结束。
- 平台1：从第1轮开始高概率标题党；配合平台治理“正常流程”验证联动（检测->健康分/流量->收益）。
- 平台2：前4轮正常；第5轮起某写手转标题党，其余写手在随后2-3轮内模仿扩散；平台先走“异常治理包顺序”，第4-8轮窗口内切回正常流程。
- 平台3：全程不使用标题党，用于验证“不开启/不命中时不应触发”的负例与边界。
- 平台4：第1轮起标题党；平台先异常后正常，用于验证“前置缺失时治理功能不应误生效/应提示”的异常路径。

## 用户agent逐轮策略（框架）

- 基础动作：登录 -> 接收推送/浏览发现与关注列表 -> 按概率互动（点赞/收藏/举报/取消关注/阅读完成）-> 如策略要求则切换平台并填写问卷 -> 结束。
- 举报策略：在需要触发“用户举报阈值”机制的平台/轮次，控制举报集中度，使其在轮次末收益结算前触发检测链路。

## 平台agent逐轮策略（框架）

- 基础动作：登录 -> 巡查 -> （按轮次策略）提交治理功能申请/取消、提交绩效方案 -> 查看生效轮次与结果页 -> 结束。
- 正常流程包：先标题党检测类（检测/举报）-> 再流量惩罚类（健康分、流量惩罚）-> 再收益类（收益惩罚）。
- 异常流程包：跳过前置（不启用检测/举报），直接启用健康分/流量/收益惩罚，用于验证“不应生效/应提示”的口径与防呆。

## 监管agent逐轮策略（框架）

- 基础动作：登录 -> 监测辖区平台 -> 按策略巡查 -> （按轮次策略）罚款/发起专项整治 -> 核对自动巡查与手动巡查区分 -> 结束。

## 管理员agent逐轮策略（框架）

- 基础动作：审批各类申请（按策略通过/驳回比例）-> 执行轮次屏障检查 -> 调用【结束轮次】推进 -> 核对轮次末流程（举报->收益惩罚->收益结算） -> 结束。

## 100轮策略时间表（按阶段给出“概率/阈值”配置）

说明：为避免逐账号逐轮清单的维护成本，采用“阶段+覆盖轮次”的策略配置。实现时由编排器在每轮开始根据 `round` 选择对应参数。

### 阶段 S0：预热与基础链路（第1～2轮）

- 写手：
  - 平台1/4：clickbait_p=0.80（大部分写手用“标题党档”）
  - 平台2：clickbait_p=0.00
  - 平台3：clickbait_p=0.00
- 平台（提交配置但不一定立刻生效）：
  - 所有平台提交绩效方案（w1/w2/w3 各平台不同；示例：平台1偏点击、平台2偏读完、平台3偏收藏、平台4均衡）。
  - 平台1按正常流程：提交并发布“标题党检测”配置与措施；随后提交/发布“健康分、流量惩罚、收益惩罚”（都将于下一轮生效）。
  - 平台4按异常流程：先尝试发布“健康分/流量惩罚/收益惩罚”，不发布标题党检测/举报（用于验证前置缺失时不应误生效/应提示）。
- 管理员：
  - 对上述提交做“部分通过、部分驳回”覆盖：至少保证每类配置出现一次 rejected（用于异常路径），并保证平台1的关键链路能在第2轮起跑通。
- 用户：
  - 按默认互动概率执行；举报维持低水平（report_p=0.02）。

### 阶段 S1：正常治理链路跑通（第3～10轮）

- 平台1（正常流程主样本）：
  - 治理启用状态：标题党检测、健康分、流量惩罚、收益惩罚均为 active（按“发布后下一轮生效”推进）。
  - 写手 clickbait_p=0.80；目标：持续产生标题党命中，覆盖扣分/降权/β惩罚与结算落库。
  - 用户举报：在第6轮触发一次“集中举报达阈值（auto审核）”，设置：
    - report_target_articles_per_platform=3
    - trigger_report_p=0.60（仅对目标文章的已读用户提高举报概率）
    - UserReportConfig：threshold=0.30，review_method=auto
- 平台2（后续扩散样本准备）：
  - 第3～4轮保持 clickbait_p=0.00；平台治理先走“异常顺序包”（例如先收益惩罚/流量惩罚尝试发布，缺前置时应不生效/提示）。
- 平台3（负例平台）：
  - 长期 clickbait_p=0.00；治理可保持关闭或只开启绩效，用于验证“不命中不处罚”。
- 平台4（异常到正常切换）：
  - 第3～4轮继续异常顺序；第5轮开始补齐标题党检测并切回正常流程（让此前的异常配置在正确前置具备后再生效）。

### 阶段 S2：扩散与策略切换（第11～30轮）

- 平台2（扩散主样本）：
  - 第11～14轮：仅 1 个写手“标题党档”固定（模拟单点变坏），其余 clickbait_p=0.10。
  - 第15～18轮：模仿扩散，整体 clickbait_p 提升到 0.50。
  - 第19～30轮：整体 clickbait_p=0.80；平台在第20轮左右从异常治理包切换为正常流程（用于验证“切换后下一轮生效/取消下一轮失效”的口径）。
  - 用户举报：在第16轮触发一次“集中举报达阈值（manual审核）”，配置：
    - UserReportConfig：threshold=0.30，review_method=manual
    - 管理员在第17轮对达到阈值的举报做“通过/驳回”各覆盖一次（通过应将文章标记标题党并联动惩罚；驳回则不联动）。
- 平台1：
  - 保持 clickbait_p=0.60（让恢复机制有机会发生）：每轮随机抽一部分写手强制正常档，形成“连续合规”以触发健康分恢复。
- 平台4：
  - 第11～18轮 clickbait_p=0.80；第19～30轮逐步下降到 0.30（覆盖“取消/降级治理后不再强惩罚”的路径）。

### 阶段 S3：监管压力与专项整治覆盖（第31～60轮）

- 监管机构：
  - 每 5 轮对辖区平台发起一次平台巡查申请（start_round/end_round 选择已过去区间，巡查比例 patrol_ratio=0.5）。
  - 对“标题党率高”的平台（预期平台1/2/4）在第35/45/55轮各触发一次罚款申请（档次覆盖 light/basic/medium/strict 至少三档）。
  - 第40轮发起一次专项整治申请（持续轮次选 8 或 12，覆盖两种时长之一），平台选择以“高标题党率平台”为主。
- 管理员：
  - 覆盖审批：巡查申请至少一次批准一次驳回；罚款申请至少一次批准一次驳回；专项整治申请至少一次批准（生成正式记录）一次驳回。
  - 验证专项整治结束后下一轮，系统自动执行两次配套巡查并落库（日志与 `PlatformPatrolResult`）。
- 平台/写手/用户：
  - 维持前述各平台 clickbait_p 模式，同时保证用户侧仍有足够互动量让巡查统计不为空。

### 阶段 S4：长跑稳定性与回归（第61～100轮）

- 目标：在不改变核心概率结构的前提下，持续覆盖边界与异常：
  - 平台端：周期性提交“无效参数/重复提交/无权限”类请求（应返回 400/403 且不污染数据），例如绩效权重和不为1、重复提交 pending。
  - 治理措施：随机挑 1 个平台做“取消措施（下一轮失效）”与“重新发布（下一轮生效）”循环各 1 次，覆盖取消语义与通知投递幂等。
  - 用户端：提高少量 switch_platform 与 unfollow 的触发频次以覆盖问卷与时间限制校验。
  - 写手端：每 10 轮引入一次“全部正常档”窗口（1轮），验证无标题党时惩罚不应触发、健康分恢复不应越界。



